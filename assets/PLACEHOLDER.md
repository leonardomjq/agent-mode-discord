# Placeholder assets

`icon.png` here is a generated placeholder (Discord blurple + "AGENT MODE" text), not the
final goblin mascot. It exists so the manifest validates and `vsce package` succeeds during
pre-publish dry-runs.

Replace with the real 128×128 mascot before tagging `v0.1.0`. Track in
`.planning/phases/05.1-polish-marketplace-prep/05.1-HUMAN-UAT.md`.

`demo.gif` is still missing — see `CAPTURE-INSTRUCTIONS.md` for the recording recipe.
