---
phase: 07-presence-v2-goblin-brand-and-watching-activity-type-lever
plan: 06
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/07-presence-v2-goblin-brand-and-watching-activity-type-lever/07-HANDOFF.md
autonomous: true
requirements: [REQ-8, REQ-9]
must_haves:
  truths:
    - "07-HANDOFF.md exists in the phase directory"
    - "HANDOFF documents the manual Discord Developer Portal app rename (Agent Mode -> goblin mode) as a deferred user action"
    - "HANDOFF documents that the package.json displayName bump is deferred to a future version-bump phase, not this phase"
    - "HANDOFF contains a render-test matrix with rows for web/desktop/mobile/friend-view x columns for Playing/Watching, with empty pass/fail cells the user fills in post-deployment"
    - "Phase verification can pass with all manual checklist items in unchecked state - code-only requirements are not gated on these"
  artifacts:
    - path: ".planning/phases/07-presence-v2-goblin-brand-and-watching-activity-type-lever/07-HANDOFF.md"
      provides: "Manual action checklist + render-test matrix for post-merge user actions"
      min_lines: 60
  key_links:
    - from: "07-HANDOFF.md"
      to: "07-SPEC.md REQ-8 + REQ-9"
      via: "deferred-action checklist + render-test matrix sections"
      pattern: "render-test matrix"
---

<objective>
Create the phase HANDOFF document that captures (1) the deferred manual brand actions (Discord Developer Portal app rename, marketplace `displayName` bump in next version) and (2) the render-test matrix the user must fill in after deploying v0.2.0 to verify Watching renders coherently across Discord clients before considering a default flip in a future phase.

Purpose: REQ-8 — manual actions tracked explicitly so they don't get lost in conversation. REQ-9 — render-test outcome captured before any default flip from `playing` → `watching`. The phase ships code-only; HANDOFF.md is the receipt for what the user owes themselves to do post-merge.

Output: New `.planning/phases/07-.../07-HANDOFF.md` file with explicit checkboxes that can stay `[ ]` without blocking the phase's git commit / verification gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-presence-v2-goblin-brand-and-watching-activity-type-lever/07-SPEC.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 07-HANDOFF.md with deferred actions + render-test matrix</name>
  <files>.planning/phases/07-presence-v2-goblin-brand-and-watching-activity-type-lever/07-HANDOFF.md</files>
  <read_first>
    - .planning/phases/07-presence-v2-goblin-brand-and-watching-activity-type-lever/07-SPEC.md (REQ-8 lines 65-68, REQ-9 lines 70-73, Boundaries lines 137-145)
    - package.json (current displayName line 3 — confirm what is being deferred)
  </read_first>
  <action>
Write the file `.planning/phases/07-presence-v2-goblin-brand-and-watching-activity-type-lever/07-HANDOFF.md` with the following content (substantively verbatim — the `***` separators below are intentional markdown horizontal rules; you may render them as `---` in the actual HANDOFF.md if preferred, since both are valid CommonMark horizontal-rule syntax. We use `***` here only because this plan file uses `---` as YAML frontmatter delimiters and embedded `---` would confuse some YAML parsers):

```markdown
# Phase 7 — Handoff: Manual Actions & Render-Test Matrix

**Created:** 2026-05-01 (auto-generated by plan 07-06)
**Spec source:** `.planning/phases/07-presence-v2-goblin-brand-and-watching-activity-type-lever/07-SPEC.md`
**Status:** Phase ships code-only. Items below are deferred user actions; phase
verification gate does NOT block on them per REQ-8 and the SPEC §Boundaries
out-of-scope list.

***

## 1. Deferred manual actions

These actions cannot be automated by the extension code path and are explicitly
out of scope for this phase's commits. Track here so they don't get lost.

### 1a. Discord Developer Portal — rename application (REQ-8)

The activity card prefix (`Playing X` / `Watching X`) shows the **Discord
application name** as `X`, not anything from the extension. To complete the
goblin brand surface, the bundled Client ID's app must be renamed in the portal.

- [ ] Open https://discord.com/developers/applications
- [ ] Select the app for the bundled `DEFAULT_CLIENT_ID` (see `src/rpc/client.ts`)
- [ ] Rename `Agent Mode` → `goblin mode`
- [ ] Save; allow up to ~5 min for Discord clients to pick up the new name
- [ ] Verify on a fresh VS Code window: card reads `Playing goblin mode` (not
      `Playing Agent Mode`) under the rotating goblin pool entry

**Why deferred:** This is a click-through action on a third-party dashboard with
no API surface bundled with the extension. Code-side requirements (REQ-1..7) are
already satisfied; this completes the user-visible brand polish.

**Fallback if not done:** Card reads `Playing Agent Mode / claude shipping
code` — still strictly better than the v0.1.3 baseline (`Playing Agent Mode /
pair-coded with AI`), so shipping without this rename is acceptable.

### 1b. Marketplace `displayName` bump — DEFERRED to v0.2.0 (REQ-8)

Per SPEC §Boundaries, the `package.json` `displayName` change from
`"Agent Mode — Rich Presence for AI Coding Agents"` to
`"goblin mode — Discord rich presence for AI coding"` (or similar) is **deferred
to the next version-bump phase**, not this phase.

Rationale: marketplace `displayName` changes require coordination with the
Marketplace re-publish cycle, README screenshot refresh, and changelog entry.
Lumping it here would force a v0.2.0 release on every brand-copy iteration.

- [ ] Track in roadmap for the next version-bump phase (post Phase 6 publish)
- [ ] When promoted, update `package.json:3` `displayName` field
- [ ] Update README screenshots + intro paragraph to match
- [ ] Bump `package.json:5` `version` per Keep a Changelog discipline
- [ ] Add CHANGELOG.md entry under the new version

**This phase MUST NOT modify `package.json:3` `displayName`.**

***

## 2. Render-test matrix (REQ-9)

The `agentMode.activityType: "watching"` setting is **opt-in only** until the
matrix below is filled in with green cells across all rows. Default
(`"playing"`) is locked until the manual render test confirms Watching renders
coherently on web + desktop + mobile + friend-view.

**Procedure** (run after `v0.2.0` is installed locally):

1. Open VS Code, start `claude` in the integrated terminal so presence flips to
   AGENT_ACTIVE.
2. For each `Mode` column: set `agentMode.activityType` to that value, reload
   the window so the next rotation tick picks up the new config (≤ 20 s).
3. For each `Surface` row: visit the corresponding Discord surface and confirm
   the card renders coherently (no truncation, prefix word matches the setting,
   no Discord-side downgrade-to-Custom).
4. Mark the cell `✓` (renders coherently) or `✗` (downgraded / truncated /
   missing) plus a short note.

| Surface                                      | Playing | Watching | Notes |
|----------------------------------------------|---------|----------|-------|
| Discord desktop client (your own profile)    | [ ]     | [ ]      |       |
| Discord desktop client (friend's view of you)| [ ]     | [ ]      |       |
| Discord web (your own profile)               | [ ]     | [ ]      |       |
| Discord web (friend's view of you)           | [ ]     | [ ]      |       |
| Discord mobile iOS                           | [ ]     | [ ]      |       |
| Discord mobile Android                       | [ ]     | [ ]      |       |

**Default-flip gate (future phase):** Only after **every Watching cell renders
coherently** may a future phase consider flipping `agentMode.activityType`
default from `"playing"` → `"watching"`. Failing cells block the flip
indefinitely; we keep the opt-in semantic and update the README troubleshooting
section to document the limitation.

***

## 3. Smoke test (post-merge, pre-publish)

Quick local verification that this phase's code-side changes work end-to-end,
even before completing the manual actions in §1 and §2.

- [ ] Pull `main` after this phase merges
- [ ] `pnpm install --frozen-lockfile && pnpm build && pnpm test`
- [ ] Launch the extension dev host (`F5` in VS Code with this repo open)
- [ ] In the dev host: open a workspace, start `claude` in integrated terminal
- [ ] Verify Discord card on your own profile reads:
      - **Title line:** `Playing Agent Mode` (until §1a is done) or
        `Playing goblin mode` (after)
      - **Details line:** rotates among the 13 locked goblin pool entries
        (e.g. `the agent shipping code`, `claude on a PR`)
      - **State line:** time-of-day modifier, e.g. `3am goblin shift` /
        `morning service` / `afternoon shift` / `evening service`
      - **Hover (large icon):** `running claude` (per agent name)
- [ ] In settings, set `agentMode.activityType` to `watching`; reload window
- [ ] Verify card title flips to `Watching X` prefix; if it doesn't render
      (Discord force-downgrade), flip back to `playing` and capture the
      failure in §2's matrix

***

## 4. SPEC compliance summary

This document satisfies the following acceptance criteria from 07-SPEC.md:

- REQ-8: Manual actions captured, NOT gated on this phase's commit. ✓
  (Discord Portal rename + displayName bump both checkboxed deferred items.)
- REQ-9: Render-test matrix exists with rows for web/desktop/mobile/friend-view
  and columns for Playing/Watching, with empty pass/fail cells filled in by
  the user post-deployment. ✓ (§2 above.)
- Phase verification can pass with all `[ ]` items unchecked. ✓ (Code-side
  requirements REQ-1..7 are independent.)

***

*Phase: 07-presence-v2-goblin-brand-and-watching-activity-type-lever*
*HANDOFF authored: 2026-05-01 by plan 07-06*
*Next step: complete §1a (Discord Portal rename) at user's discretion; complete
§2 matrix after v0.2.0 installs locally.*
```

This file is markdown only — no code, no runtime impact. It exists to capture
state that would otherwise live only in conversation memory.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/07-presence-v2-goblin-brand-and-watching-activity-type-lever/07-HANDOFF.md'; const s=fs.readFileSync(p,'utf8'); const checks=['Discord Developer Portal','goblin mode','displayName','Render-test matrix','Playing','Watching','desktop','web','mobile','friend','REQ-8','REQ-9']; const missing=checks.filter(c=>!s.includes(c)); if(missing.length) throw new Error('missing: '+missing.join(', ')); if(s.length<2000) throw new Error('too short: '+s.length); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-presence-v2-goblin-brand-and-watching-activity-type-lever/07-HANDOFF.md` exists.
    - File contains the substring `Discord Developer Portal` (REQ-8 portal-rename action).
    - File contains the substring `goblin mode` (target name for the rename).
    - File contains the substring `displayName` (deferred marketplace bump).
    - File contains a render-test matrix with explicit rows for `desktop`, `web`, `mobile`, `friend` AND explicit columns for `Playing` and `Watching`.
    - Render-test matrix has at least 6 rows of `[ ]` checkboxes (one per surface × 2 modes).
    - File contains explicit references to REQ-8 and REQ-9 (traceability).
    - File total length > 2000 chars (content depth, not a stub).
    - File DOES NOT instruct any code change to `package.json:3` `displayName` in this phase (the deferral note is explicit).
    - File renders cleanly as markdown (no broken frontmatter, no unclosed code fences) — verified by `pnpm exec markdown-link-check` if available, otherwise manual eyeball.
  </acceptance_criteria>
  <done>HANDOFF.md authored with three sections (deferred actions, render-test matrix, smoke test) and an explicit SPEC compliance summary referencing REQ-8 and REQ-9; user has a single document to consult for post-merge actions.</done>
</task>

</tasks>

<verification>
- File exists at the documented path.
- Contains all locked sections (deferred actions, matrix, smoke test, SPEC summary).
- Phase verification gate (whatever runs after `/gsd-execute-phase 7`) treats unchecked HANDOFF items as deferred-acceptable, not blocking.
</verification>

<success_criteria>
- REQ-8 satisfied: deferred manual actions (Discord Developer Portal rename, marketplace displayName bump) tracked with explicit checkboxes; phase commits do NOT modify `package.json:3` displayName.
- REQ-9 satisfied: render-test matrix exists with web/desktop/mobile/friend-view rows × Playing/Watching columns; cells start empty for the user to fill in post-v0.2.0 install.
- HANDOFF can stand alone — a future contributor or the user themselves can pick up this document a month later and know exactly what manual actions remain.
</success_criteria>

<output>
After completion, create `.planning/phases/07-presence-v2-goblin-brand-and-watching-activity-type-lever/07-06-SUMMARY.md` per template.
</output>
