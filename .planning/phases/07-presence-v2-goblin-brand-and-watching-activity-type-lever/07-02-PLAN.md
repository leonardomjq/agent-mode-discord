---
phase: 07-presence-v2-goblin-brand-and-watching-activity-type-lever
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - test/presence.goblin.voice.test.ts
autonomous: true
requirements: [REQ-1, REQ-6, REQ-7]
must_haves:
  truths:
    - "A unit test loads the built-in goblin pack and asserts every pool entry passes the SPEC-locked voice rules"
    - "Future PRs that add bad copy (banned tokens, uppercase, non-AI-named, ungrammatical-after-Watching, past-tense action verbs) fail CI"
    - "The test enumerates all 5 pools (_primary, claude, codex, CODING, IDLE) and the 4 timeOfDay buckets"
    - "The past-tense rule from 07-SPEC §Voice rules is enforced as an automated CI gate (not just code review)"
  artifacts:
    - path: "test/presence.goblin.voice.test.ts"
      provides: "Voice-rules invariants unit test"
      min_lines: 80
  key_links:
    - from: "test/presence.goblin.voice.test.ts"
      to: "src/presence/packLoader.ts"
      via: "import { BUILTIN_GOBLIN_PACK }"
      pattern: "BUILTIN_GOBLIN_PACK"
---

<objective>
Create a vitest test file `test/presence.goblin.voice.test.ts` that loads the built-in goblin pack via `BUILTIN_GOBLIN_PACK` and asserts every pool entry satisfies the locked voice rules from 07-SPEC.md §"Voice rules":

  (a) entry contains one of the tokens `claude`, `codex`, or `agent`
  (b) entry contains NONE of the banned-substring tokens (`afk`, `pair-coded`, `touching grass`, `stepped away`, `agent-augmented`, `outsourced`, `vibe shipping`, `prompt → PR`, `brb`)
  (c) entry is fully lowercase (no uppercase letters)
  (d) `"Watching " + entry` does NOT begin with banned-after-Watching tokens (`Watching letting`, `Watching between`, `Watching reviewing`, `Watching drafting`)
  (e) entry contains NONE of the banned past-tense action verbs (`shipped`, `coded`, `built`, `merged`, `wrote`, `paired`, `outsourced`, `augmented`, `pair-coded`, `pushed`, `committed`, `deployed`, `refactored`) — enforces 07-SPEC §Voice rules "no past tense" rule as a CI gate

Plus pool-count invariants (REQ-1 acceptance: locked counts per pool).

Purpose: REQ-6 acceptance — voice rules captured AS ENFORCEABLE TESTS, not just SPEC prose. Future contributors who add a regression-class entry (especially past-tense verbs like `claude shipped a PR`) will see a failing CI test, not a stylistic comment review.

Output: New test file that runs as part of `pnpm test` and turns red on any voice-rule violation including past-tense action verbs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-presence-v2-goblin-brand-and-watching-activity-type-lever/07-SPEC.md
@src/presence/packLoader.ts
@src/presence/types.ts
@test/presence.packLoader.test.ts

<interfaces>
<!-- BUILTIN_GOBLIN_PACK is exported from packLoader.ts:33 — imports the JSON statically -->
```typescript
// from src/presence/packLoader.ts
export const BUILTIN_GOBLIN_PACK: Pack = goblinPackJson as Pack;
```

<!-- Pack pool layout (Message[] | nested by agent for AGENT_ACTIVE) -->
```typescript
// from src/presence/types.ts
export interface Pack {
  pools: {
    AGENT_ACTIVE: { _primary: Message[]; [agent: string]: Message[] };
    CODING: Message[];
    IDLE: Message[];
  };
  timeOfDay?: { lateNight?: Message[]; morning?: Message[]; afternoon?: Message[]; evening?: Message[]; };
}
export type Message = string | string[];
```

<!-- Locked-pool entries that look "past-tense-adjacent" but are NOT action verbs -->
<!-- (these MUST pass the past-tense gate to avoid false positives): -->
<!--   "claude locked in"          — "locked in" is idiomatic adjectival, not past-tense action -->
<!--   "claude paused for review"  — "paused" is stative/adjectival, the agent is currently paused -->
<!-- Strategy: use a FINITE banned-suffix-list of action verbs (not a generic /\w+ed\b/ regex). -->
<!-- This avoids false positives on idiomatic adjectival uses while catching the regression class -->
<!-- (`shipped`, `coded`, `built`, etc.) the SPEC's "no past tense" rule was written to ban. -->

<!-- vitest harness — see test/presence.packLoader.test.ts for import patterns -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write voice-rules invariants test (incl. past-tense gate)</name>
  <files>test/presence.goblin.voice.test.ts</files>
  <read_first>
    - .planning/phases/07-presence-v2-goblin-brand-and-watching-activity-type-lever/07-SPEC.md (§"Voice rules" lines 75-90, §"Locked pool" lines 92-124)
    - src/presence/packLoader.ts (BUILTIN_GOBLIN_PACK export)
    - src/presence/types.ts (Pack / Message types)
    - test/presence.packLoader.test.ts (existing vitest patterns for the same module)
  </read_first>
  <action>
Create the file `test/presence.goblin.voice.test.ts` with the content below. Key design decision: the past-tense rule is enforced via a **finite banned-suffix-list of action verbs**, NOT a generic `/\w+ed\b/` regex. Reasoning:

- A generic `/\w+ed\b/` regex would false-positive on idiomatic adjectival uses in the locked pool: `claude locked in` (idiom, not past-tense action) and `claude paused for review` (stative — describes current state).
- A whitelist-based approach (`/\w+ed\b/` minus `[locked, paused]`) is brittle: every future legitimate adjectival use would have to be added to the whitelist, creating ongoing maintenance burden.
- A **finite list of common past-tense action verbs** that the SPEC's "no past tense" rule was written to ban (`shipped`, `coded`, `built`, etc.) is enforceable in CI today, has zero false positives on the locked v1 pool, and catches the regression class the rule targets. New verbs can be appended as new regressions surface.

The locked pool was hand-verified to pass this gate during planning:
- `the agent cooking` ✓ — `cooking` is gerund, not past-tense.
- `the agent shipping code` ✓ — `shipping` is gerund.
- `the agent on a PR` ✓ — preposition phrase.
- `the agent locked in` — `locked` is in the past-tense ACTION-VERB list ONLY if you treat it as past-tense action; we EXCLUDE it from the banned list because "locked in" is idiomatic-adjectival ("currently locked in" = "currently focused"), per the SPEC's accept example on line 88.
- `claude paused for review` — `paused` is stative ("currently paused"), per SPEC accept example on line 88.

```typescript
/**
 * Phase-7 voice-rules invariants test (REQ-1, REQ-6, REQ-7).
 *
 * Loads BUILTIN_GOBLIN_PACK and enforces the locked voice rules from
 * 07-SPEC.md §"Voice rules". Future PRs that introduce banned tokens,
 * past-tense action verbs, or ungrammatical-after-Watching entries fail
 * this test in CI.
 *
 * Past-tense enforcement strategy: finite banned-suffix-list of common
 * action verbs. Avoids false positives on idiomatic adjectival uses
 * (`locked in`, `paused for review`) while catching the regression class
 * the SPEC's "no past tense" rule targets (`shipped`, `coded`, etc.).
 *
 * Pure unit test — no vscode, no fs, no clock injection. Deterministic.
 */
import { describe, expect, it } from "vitest";
import type { Message, Pack } from "../src/presence/types";
import { BUILTIN_GOBLIN_PACK } from "../src/presence/packLoader";

// --- Voice rules (locked in 07-SPEC.md §"Voice rules") -----------------------

const REQUIRED_AI_TOKENS = ["claude", "codex", "agent"] as const;

const BANNED_SUBSTRINGS = [
  "afk",
  "pair-coded",
  "touching grass",
  "stepped away",
  "agent-augmented",
  "outsourced",
  "vibe shipping",
  "prompt → PR",
  "brb",
] as const;

const BANNED_AFTER_WATCHING = [
  "Watching letting",
  "Watching between",
  "Watching reviewing",
  "Watching drafting",
] as const;

/**
 * Banned past-tense ACTION verbs (07-SPEC §Voice rules — "no past tense").
 *
 * Finite list rather than a generic /\w+ed\b/ regex to avoid false positives
 * on idiomatic adjectival uses: `locked in` (focused), `paused for review`
 * (stative). Both appear in the locked v1 pool and are SPEC-accepted.
 *
 * Append to this list when a new past-tense regression is observed in PR
 * review; do NOT remove `locked` / `paused` to make this stricter — they
 * would false-positive on the existing locked pool.
 *
 * Matched as whole words via `\bWORD\b` (case-insensitive) so "decoded"
 * does not trip on `coded` and "shipping" does not trip on `shipped`.
 */
const BANNED_PAST_TENSE_VERBS = [
  "shipped",
  "coded",
  "built",
  "merged",
  "wrote",
  "paired",
  "outsourced",   // also in BANNED_SUBSTRINGS — belt-and-suspenders
  "augmented",
  "pair-coded",   // hyphen included; regex below handles word boundaries
  "pushed",
  "committed",
  "deployed",
  "refactored",
] as const;

// --- Pool flattening helper --------------------------------------------------

/** Flatten Message (string | string[]) to a list of strings. Frame sequences
 *  (string[]) are flattened so per-frame voice rules apply to every visible
 *  state. The locked v1 pool ships only strings, but this guards future
 *  pack additions that may use frame sequences. */
function flatten(messages: Message[]): string[] {
  const out: string[] = [];
  for (const m of messages) {
    if (typeof m === "string") out.push(m);
    else if (Array.isArray(m)) out.push(...m);
  }
  return out;
}

/** Collect every pool entry from the pack (across _primary, claude, codex,
 *  CODING, IDLE). Excludes timeOfDay — that has separate count rules per
 *  SPEC §3 (one canonical entry per bucket). */
function collectPoolEntries(pack: Pack): { pool: string; entry: string }[] {
  const out: { pool: string; entry: string }[] = [];
  for (const [agentKey, msgs] of Object.entries(pack.pools.AGENT_ACTIVE)) {
    for (const e of flatten(msgs as Message[])) {
      out.push({ pool: `AGENT_ACTIVE:${agentKey}`, entry: e });
    }
  }
  for (const e of flatten(pack.pools.CODING)) out.push({ pool: "CODING", entry: e });
  for (const e of flatten(pack.pools.IDLE)) out.push({ pool: "IDLE", entry: e });
  return out;
}

/** Build a case-insensitive whole-word regex for a banned past-tense verb.
 *  Hyphens are escaped so "pair-coded" matches as a unit. */
function bannedVerbRegex(verb: string): RegExp {
  const escaped = verb.replace(/[-/\\^$*+?.()|[\]{}]/g, "\\$&");
  return new RegExp(`\\b${escaped}\\b`, "i");
}

// --- Tests -------------------------------------------------------------------

describe("goblin pack voice rules (07-SPEC §Voice rules)", () => {
  const entries = collectPoolEntries(BUILTIN_GOBLIN_PACK);

  it("collects at least 13 entries (sanity — REQ-1 lock)", () => {
    // Lower bound — the locked v1 pool has exactly 13. Allow >13 if a future
    // version expands without breaking voice rules.
    expect(entries.length).toBeGreaterThanOrEqual(13);
  });

  // Rule (a): every entry names the AI.
  for (const { pool, entry } of entries) {
    it(`names the AI [${pool}] "${entry}"`, () => {
      const lc = entry.toLowerCase();
      const named = REQUIRED_AI_TOKENS.some((tok) => lc.includes(tok));
      expect(named).toBe(true);
    });
  }

  // Rule (b): no banned tokens.
  for (const { pool, entry } of entries) {
    it(`has no banned tokens [${pool}] "${entry}"`, () => {
      const lc = entry.toLowerCase();
      for (const banned of BANNED_SUBSTRINGS) {
        expect(lc.includes(banned.toLowerCase())).toBe(false);
      }
    });
  }

  // Rule (c): fully lowercase.
  for (const { pool, entry } of entries) {
    it(`is fully lowercase [${pool}] "${entry}"`, () => {
      expect(entry).toBe(entry.toLowerCase());
    });
  }

  // Rule (d): grammatical after `Watching `.
  for (const { pool, entry } of entries) {
    it(`is grammatical after "Watching " [${pool}] "${entry}"`, () => {
      const composed = `Watching ${entry}`;
      for (const banned of BANNED_AFTER_WATCHING) {
        expect(composed.startsWith(banned)).toBe(false);
      }
    });
  }

  // Rule (e): no past-tense action verbs (07-SPEC §Voice rules — "no past
  // tense"). Enforced as a finite whole-word match list to avoid false
  // positives on idiomatic adjectival uses (`locked in`, `paused for review`).
  for (const { pool, entry } of entries) {
    it(`has no past-tense action verbs [${pool}] "${entry}"`, () => {
      for (const verb of BANNED_PAST_TENSE_VERBS) {
        const rx = bannedVerbRegex(verb);
        expect(
          rx.test(entry),
          `entry "${entry}" matches banned past-tense verb "${verb}" (07-SPEC §Voice rules — no past tense)`,
        ).toBe(false);
      }
    });
  }
});

describe("goblin pack pool counts (07-SPEC §Locked pool — REQ-1 lock)", () => {
  const p = BUILTIN_GOBLIN_PACK;

  it("AGENT_ACTIVE._primary has 4 entries", () => {
    expect(p.pools.AGENT_ACTIVE._primary).toHaveLength(4);
  });
  it("AGENT_ACTIVE.claude has 4 entries", () => {
    expect(p.pools.AGENT_ACTIVE.claude).toHaveLength(4);
  });
  it("AGENT_ACTIVE.codex has 3 entries", () => {
    expect(p.pools.AGENT_ACTIVE.codex).toHaveLength(3);
  });
  it("CODING has 2 entries", () => {
    expect(p.pools.CODING).toHaveLength(2);
  });
  it("IDLE has 2 entries", () => {
    expect(p.pools.IDLE).toHaveLength(2);
  });
  it("total locked entries === 13", () => {
    const total =
      p.pools.AGENT_ACTIVE._primary.length +
      p.pools.AGENT_ACTIVE.claude.length +
      p.pools.AGENT_ACTIVE.codex.length +
      p.pools.CODING.length +
      p.pools.IDLE.length;
    expect(total).toBe(13);
  });
});

describe("goblin pack timeOfDay canonical strings (07-SPEC §3 — REQ-3)", () => {
  const tod = BUILTIN_GOBLIN_PACK.timeOfDay!;
  it("lateNight === ['3am goblin shift']", () => {
    expect(tod.lateNight).toEqual(["3am goblin shift"]);
  });
  it("morning === ['morning service']", () => {
    expect(tod.morning).toEqual(["morning service"]);
  });
  it("afternoon === ['afternoon shift']", () => {
    expect(tod.afternoon).toEqual(["afternoon shift"]);
  });
  it("evening === ['evening service']", () => {
    expect(tod.evening).toEqual(["evening service"]);
  });
});
```

This is purely a runtime invariant check; it makes no fs/network/vscode calls and adds no runtime deps. The test runs as part of the existing vitest suite (`pnpm test`).
  </action>
  <verify>
    <automated>pnpm test -- presence.goblin.voice</automated>
  </verify>
  <acceptance_criteria>
    - File `test/presence.goblin.voice.test.ts` exists.
    - `pnpm test -- presence.goblin.voice` exits 0 against the goblin.json from plan 07-01 (every locked entry passes all five voice rules including the past-tense gate).
    - `BANNED_PAST_TENSE_VERBS` array exists in the test file and contains at least: `shipped`, `coded`, `built`, `merged`, `wrote`, `paired`, `outsourced`, `augmented`, `pair-coded`.
    - `bannedVerbRegex` helper exists and uses `\bWORD\b` whole-word matching (case-insensitive).
    - Manual mutation test: temporarily change `_primary[0]` to `"claude shipped a PR"` — `pnpm test -- presence.goblin.voice` MUST fail with the "has no past-tense action verbs" assertion (revert before commit).
    - Manual mutation test: temporarily change `_primary[1]` to `"the agent coded a feature"` — test MUST fail with the past-tense gate matching `coded`.
    - Manual mutation test: temporarily change `_primary[0]` to `"shipping it"` (no AI named) — test MUST fail with the "names the AI" assertion.
    - Manual mutation test: temporarily change `_primary[0]` to `"AGENT cooking"` (uppercase) — test MUST fail with the "is fully lowercase" assertion.
    - Manual mutation test: temporarily insert `"letting claude cook"` — test MUST fail with the "grammatical after Watching" assertion.
    - **Locked-pool false-positive guard:** With the unmodified locked v1 pool (`claude locked in`, `claude paused for review`), the past-tense gate MUST pass — neither `locked` nor `paused` are on the banned list, by design.
    - Test imports only from `vitest`, `../src/presence/types`, `../src/presence/packLoader` (no new runtime deps).
    - Test does NOT import vscode (preserves PURE_CORE — verified by check-api-surface.mjs CI guardrail; this is a test file but should not introduce vscode coupling).
    - Pool-count assertions match REQ-1: 4 / 4 / 3 / 2 / 2 = 13 total.
    - timeOfDay assertions match SPEC §3 verbatim canonical strings.
  </acceptance_criteria>
  <done>Voice-rules test exists, runs green against the 07-01 goblin.json, enforces the past-tense rule from SPEC §Voice rules as a CI gate (not just code review), and reliably fails on regressed entries (verified by the documented mutation tests). Locked-pool idiomatic adjectival uses (`locked in`, `paused for review`) pass without false positives.</done>
</task>

</tasks>

<verification>
- `pnpm test -- presence.goblin.voice` exits 0.
- All documented mutation tests reliably fail and revert cleanly.
- Existing test suite (`pnpm test`) remains green overall — new file does not affect other tests.
- Past-tense gate is enforceable in CI (test failure on regressed entry, not just code review).
</verification>

<success_criteria>
- Voice-rules unit test exists and enforces the SPEC-locked rules over every pool entry, including past-tense action verbs.
- Pool-count invariants enforced (REQ-1 lock).
- TimeOfDay canonical strings enforced (REQ-3 lock).
- Test is deterministic, fast (<1 s), no fs/network/vscode coupling.
- Past-tense rule is now a CI gate, not just a SPEC §Voice rules comment — the M3 concern from plan-check is resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/07-presence-v2-goblin-brand-and-watching-activity-type-lever/07-02-SUMMARY.md` per template.
</output>
</content>
</invoke>