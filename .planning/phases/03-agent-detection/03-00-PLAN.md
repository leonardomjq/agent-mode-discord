---
phase: 03
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/detectors.shellIntegration.test.ts
  - test/detectors.sessionFiles.test.ts
  - test/detectors.polling.test.ts
  - test/detectors.index.test.ts
  - test/detectors.regex.test.ts
  - test/detectors/__helpers__/fakeTerminal.ts
  - test/detectors/__helpers__/ansiFixtures.ts
  - scripts/check-api-surface.mjs
  - .planning/phases/03-agent-detection/03-HUMAN-UAT.md
autonomous: true
requirements: [DET-01, DET-02, DET-03, DET-04, DET-05, DET-06, DET-07, DET-08, DET-09, DET-10]
tags: [test-scaffolding, wave-0, fixtures, api-surface-guard, human-uat]

must_haves:
  truths:
    - "All 5 Phase-3 detector test files exist with full it.todo stubs (one stub per behavioral assertion downstream plans will flip)"
    - "Two helper modules (fakeTerminal + ansiFixtures) exist and export the factories listed in 03-VALIDATION.md §Fixture Strategy"
    - "scripts/check-api-surface.mjs PURE_CORE_PATHS includes src/detectors/regex.ts so any future vscode import there fails CI"
    - "03-HUMAN-UAT.md exists with SC-3.1 through SC-3.8 manual checklist items copied from VALIDATION"
    - "pnpm test exits 0 with all new tests as pending todos (no implementation yet — Wave 1 will flip them)"
    - "pnpm check:api-surface PASS with the new regex.ts entry in PURE_CORE_PATHS (file does not yet exist; checker tolerates absence)"
  artifacts:
    - path: "test/detectors.shellIntegration.test.ts"
      provides: "Stubs for DET-01 / DET-04 / DET-08 / DET-09 (shell integration adapter)"
      contains: "describe(\"shellIntegration detector\""
    - path: "test/detectors.sessionFiles.test.ts"
      provides: "Stubs for DET-05 (fs.watch + 60s staleness threshold)"
      contains: "describe(\"sessionFiles detector\""
    - path: "test/detectors.polling.test.ts"
      provides: "Stubs for DET-06 (5s interval, empty-by-default)"
      contains: "describe(\"polling detector\""
    - path: "test/detectors.index.test.ts"
      provides: "Stubs for DET-07 / DET-04 aggregation (precedence orchestrator)"
      contains: "describe(\"detectors orchestrator\""
    - path: "test/detectors.regex.test.ts"
      provides: "Stubs for DET-02 / DET-03 / DET-09 / DET-10 (regex module)"
      contains: "describe(\"regex matcher\""
    - path: "test/detectors/__helpers__/fakeTerminal.ts"
      provides: "makeFakeTerminal({ name?, hasShellIntegration?, activateShellIntegrationAfterMs? }) returning { terminal, emit }"
      contains: "export function makeFakeTerminal"
    - path: "test/detectors/__helpers__/ansiFixtures.ts"
      provides: "LOW_CONFIDENCE_FIXTURES array — 5+ shell variants per VALIDATION fixture spec"
      contains: "export const LOW_CONFIDENCE_FIXTURES"
    - path: "scripts/check-api-surface.mjs"
      provides: "Updated PURE_CORE_PATHS array including src/detectors/regex.ts"
      contains: "src/detectors/regex.ts"
    - path: ".planning/phases/03-agent-detection/03-HUMAN-UAT.md"
      provides: "SC-3.1..SC-3.8 manual verification checklist for Dev Host + real Claude Code"
      contains: "SC-3.1"
  key_links:
    - from: "test/detectors.shellIntegration.test.ts"
      to: "test/detectors/__helpers__/fakeTerminal.ts"
      via: "import { makeFakeTerminal }"
      pattern: "import.*makeFakeTerminal.*__helpers__/fakeTerminal"
    - from: "test/detectors.regex.test.ts"
      to: "test/detectors/__helpers__/ansiFixtures.ts"
      via: "import { LOW_CONFIDENCE_FIXTURES }"
      pattern: "import.*LOW_CONFIDENCE_FIXTURES.*__helpers__/ansiFixtures"
    - from: "scripts/check-api-surface.mjs"
      to: "src/detectors/regex.ts"
      via: "PURE_CORE_PATHS array entry"
      pattern: "PURE_CORE_PATHS.*src/detectors/regex.ts"
---

<objective>
Wave 0 test scaffolding for Phase 3. Creates 5 new test stub files (one per detector module), 2 fixture helpers (fake vscode.Terminal factory, ANSI/Low-confidence fixtures), extends scripts/check-api-surface.mjs PURE_CORE_PATHS so src/detectors/regex.ts cannot accidentally import vscode at runtime, and writes 03-HUMAN-UAT.md with the SC-3.1..SC-3.8 checklist from VALIDATION.

Purpose: Nyquist compliance for Wave 1+. Every Wave 1 task in plans 03-01..03-05 will flip it.todo placeholders into passing tests; this plan creates those placeholders so the executor never has to author scaffolding while implementing. Also locks the PURE_CORE guard before any code is written, preventing accidental vscode-coupling of the regex module.

Output:
- 5 new test files (all containing only `it.todo(...)` placeholders — pnpm test green, no flips)
- 2 helper modules under test/detectors/__helpers__/
- scripts/check-api-surface.mjs PURE_CORE_PATHS extended
- 03-HUMAN-UAT.md authored
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-agent-detection/03-CONTEXT.md
@.planning/phases/03-agent-detection/03-RESEARCH.md
@.planning/phases/03-agent-detection/03-VALIDATION.md
@scripts/check-api-surface.mjs
@test/detectors.editor.test.ts
@test/detectors.git.test.ts
</context>

<interfaces>
<!-- Helper shapes (test/detectors/__helpers__/) — these are CONTRACTS Wave 1 will consume. -->

```typescript
// test/detectors/__helpers__/fakeTerminal.ts
import type * as vscode from "vscode";

export interface FakeTerminalHandle {
  terminal: vscode.Terminal;
  /** Fire a simulated event into observers registered on this fake terminal. */
  emit(event:
    | "shellIntegrationActivated"
    | "executionStart"
    | "executionEnd"
    | "close",
    payload?: unknown,
  ): void;
}

export function makeFakeTerminal(opts?: {
  name?: string;
  hasShellIntegration?: boolean;
  activateShellIntegrationAfterMs?: number;
}): FakeTerminalHandle;

// test/detectors/__helpers__/ansiFixtures.ts
export interface LowConfidenceFixture {
  shell: "bash" | "zsh" | "fish" | "powershell" | "bash-raw";
  raw: string;
  expected: string; // normalized command line after strip pipeline
}

export const LOW_CONFIDENCE_FIXTURES: LowConfidenceFixture[];
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create fixture helpers under test/detectors/__helpers__/</name>
  <files>test/detectors/__helpers__/fakeTerminal.ts, test/detectors/__helpers__/ansiFixtures.ts</files>
  <action>
    Create two helper modules.

    **test/detectors/__helpers__/fakeTerminal.ts** — minimal fake vscode.Terminal + shellIntegration factory.
    Implement `makeFakeTerminal(opts?)` returning `{ terminal, emit(event, payload?) }`. Use the shape in `<interfaces>`.
    The factory must:
      - Construct an object cast to `vscode.Terminal` (use `as unknown as vscode.Terminal`).
      - Track per-terminal observers in a private map keyed by event name.
      - Initialize `terminal.shellIntegration = undefined` if `hasShellIntegration === false` (default), else a stub object with the minimum surface vscode.TerminalShellIntegration requires (use `as unknown as vscode.TerminalShellIntegration`).
      - If `activateShellIntegrationAfterMs > 0`, schedule a `setTimeout` that flips `terminal.shellIntegration` to the stub and emits `shellIntegrationActivated`.
      - `emit('executionStart', { commandLine: { value, confidence } })` invokes any registered listener.
      - `emit('executionEnd', { exitCode })` invokes any end listener.
      - `emit('close')` invokes close listeners.
      - Pure TS, no vitest mocks here — this is a pure factory consumed BY tests.

    **test/detectors/__helpers__/ansiFixtures.ts** — the LOW_CONFIDENCE_FIXTURES array.
    Verbatim copy from 03-VALIDATION.md §Fixture Strategy plus the additional Low-confidence cases enumerated in 03-RESEARCH.md §Validation Architecture (the ANSI_FIXTURES table — at least 13 entries covering every shell variant, sudo/env prefixes, hyphen-binary negative case, python -m aider, npx/bunx variants).
    Each entry: `{ shell, raw, expected }`. `expected` = string after stripAnsiCsi + prompt-prefix-strip + multi-space-collapse + trim. For negative cases (no agent), set `expected` to the normalized command line (the test asserts matchAgentCommand returns null on it).

    Both files MUST compile under `pnpm typecheck` (these are .ts files in the test folder; they'll be picked up by tsconfig).
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
    Both files exist; pnpm typecheck exits 0; no runtime imports beyond the (type-only) vscode import in fakeTerminal.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 5 detector test stub files (it.todo only)</name>
  <files>test/detectors.shellIntegration.test.ts, test/detectors.sessionFiles.test.ts, test/detectors.polling.test.ts, test/detectors.index.test.ts, test/detectors.regex.test.ts</files>
  <action>
    Create 5 test files, each containing ONLY `describe` blocks with `it.todo(...)` entries — no implementation, no `vi.mock`. Wave 1 plans will flip these todos.

    Each file's `describe()` headline + the `it.todo()` titles MUST cover the per-requirement assertions in 03-VALIDATION.md §Per-Requirement Verification Map. Suggested stub-count per file:

    **test/detectors.shellIntegration.test.ts** (DET-01 / DET-04 / DET-08 / DET-09):
      - it.todo("dispatches agent-started within 500ms when claude command starts (DET-01)")
      - it.todo("normalizes Low-confidence commandLine via ANSI strip + prompt-prefix strip (DET-09)")
      - it.todo("subscribes onDidChangeTerminalShellIntegration globally for async activation (DET-08)")
      - it.todo("seeds existing terminals with active shell integration at start (Pitfall 1)")
      - it.todo("starts 2000 ms holdoff for terminals without shell integration at construction (DET-08)")
      - it.todo("cancels holdoff timer when shell integration activates within window (DET-08)")
      - it.todo("starts 30s grace on agent-end and dispatches agent-ended only when grace expires")
      - it.todo("cancels pending grace timer when same-terminal agent-started fires inside grace (Pitfall 2)")
      - it.todo("two parallel claude sessions in two terminals each tracked independently (DET-04)")
      - it.todo("onDidCloseTerminal deletes session immediately, no grace (Pitfall 3)")

    **test/detectors.sessionFiles.test.ts** (DET-05):
      - it.todo("dispatches agent-started when ~/.claude/projects/<cwd>/<uuid>.jsonl mtime updates (DET-05)")
      - it.todo("ignores files older than sessionFileStalenessSeconds (default 60s)")
      - it.todo("debounces fs.watch double-fire within 100ms (Pitfall 4 macOS)")
      - it.todo("falls back to 5s polling-stat loop on Linux (Pitfall 4 platform branch)")
      - it.todo("uses recursive: true on macOS and Windows (process.platform check)")
      - it.todo("never reads JSONL content — only fs.statSync().mtimeMs (PRD §FR-1.8)")
      - it.todo("silent on missing ~/.claude/projects/ directory at startup (D-18)")

    **test/detectors.polling.test.ts** (DET-06):
      - it.todo("does not poll when detect.polling.terminalNamePatterns is empty (DET-06 default)")
      - it.todo("polls vscode.window.terminals every 5s when patterns non-empty")
      - it.todo("dispatches agent-started for terminals matching any configured pattern")
      - it.todo("does not dispatch when higher tier already signals same terminal (no-op coordination)")
      - it.todo("disposes interval cleanly on dispose")

    **test/detectors.index.test.ts** (DET-07 + DET-04 aggregation):
      - it.todo("starts all detectors in tier order on start()")
      - it.todo("higher tier signal suppresses lower tier signal for same terminal at debug-log only (DET-07)")
      - it.todo("agent label = highest-tier active session (tie → most recent lastActivityAt)")
      - it.todo("AGENT_ACTIVE while ANY session has signalTier > 0 OR is in grace (DET-04 aggregation)")
      - it.todo("dispatches agent-started exactly once on first session, agent-ended exactly once on last session end")
      - it.todo("dispatches agent-started with new agent label when highest-tier session changes agent")
      - it.todo("disposes all child detectors on dispose")

    **test/detectors.regex.test.ts** (DET-02 / DET-03 / DET-09 / DET-10):
      - it.todo("matches `claude` and `claude --print hi` to agent=claude (DET-01/02)")
      - it.todo("matches npx/bunx/pnpm dlx @anthropic-ai/claude-code variants (DET-02)")
      - it.todo("matches aider, python -m aider, python3 -m aider (DET-03)")
      - it.todo("matches codex, npx @openai/codex, bunx @openai/codex (DET-03)")
      - it.todo("matches gemini and opencode (DET-03)")
      - it.todo("admin subcommands (claude --help, claude mcp list, claude --version) still match (CONTEXT)")
      - it.todo("does NOT match `git commit -m \"fix claude\"` (claude is argument)")
      - it.todo("does NOT match `./claude-history.sh` (hyphen breaks word boundary)")
      - it.todo("strips ANSI CSI sequences from Low-confidence commandLine (DET-09)")
      - it.todo("strips prompt prefixes [user@host path] $, %, ❯, →, $, ▶ (DET-09)")
      - it.todo("collapses multiple spaces and trims (DET-09)")
      - it.todo("does NOT strip Medium/High confidence values (DET-09)")
      - it.todo("detect.customPatterns extends built-ins; user pattern claude-next auto-prefixed with ^ (DET-10)")
      - it.todo("custom pattern unknown agent name flows through to agent label (DET-10)")
      - it.todo("LOW_CONFIDENCE_FIXTURES table — every fixture normalizes and matches its expected agent")

    Files MUST import vitest (`import { describe, it } from \"vitest\"`) but MUST NOT yet import the source modules under test (those don't exist yet). Compile must pass.
  </action>
  <verify>
    <automated>pnpm test --run 2>&1 | grep -E "(passed|todo|failed)"</automated>
  </verify>
  <done>
    All 5 test files exist; pnpm test exits 0; output shows the new it.todo entries (Phase 1 + Phase 2 tests still pass); no flipped tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend scripts/check-api-surface.mjs PURE_CORE_PATHS + write 03-HUMAN-UAT.md</name>
  <files>scripts/check-api-surface.mjs, .planning/phases/03-agent-detection/03-HUMAN-UAT.md</files>
  <action>
    **Edit scripts/check-api-surface.mjs**: add `"src/detectors/regex.ts"` to the `PURE_CORE_PATHS` array (after `"src/privacy.ts"`). The current array contains: `["src/state/", "src/rpc/throttle.ts", "src/privacy.ts"]`. After: `["src/state/", "src/rpc/throttle.ts", "src/privacy.ts", "src/detectors/regex.ts"]`.

    Do NOT add `src/detectors/` (broad path) — the other detector files (shellIntegration.ts, sessionFiles.ts, polling.ts, index.ts) MUST import vscode. Only the regex module is pure-core.

    The check must still pass even when `src/detectors/regex.ts` does not yet exist — the checker walks the actual src tree, so a file that doesn't exist isn't scanned. Verify with `pnpm check:api-surface`.

    **Create .planning/phases/03-agent-detection/03-HUMAN-UAT.md** with this structure:

    ```markdown
    ---
    phase: "03"
    slug: agent-detection
    status: pending
    ---

    # Phase 03 Agent Detection — Human UAT Checklist

    All items require running the unpacked extension in a Dev Host (F5) AND a real Claude Code install on this machine. Do not sign off remotely.

    ## Checklist 1 — Shell Integration tier (DET-01, DET-02, DET-04)

    - [ ] **SC-3.1** Launch Dev Host (F5). Open integrated terminal (zsh on macOS preferred). Run `claude`. Verify Discord profile flips to AGENT_ACTIVE within 500 ms (use a stopwatch or Discord profile timer).
    - [ ] **SC-3.2** With `claude` REPL active, send 3-5 user prompts. Verify Discord stays AGENT_ACTIVE across the entire session — no flicker to CODING during tool calls or model responses.
    - [ ] **SC-3.3** Ctrl+C the `claude` REPL. Start a stopwatch. Verify Discord HOLDS AGENT_ACTIVE for 30 s (grace), then downgrades to CODING/IDLE.
    - [ ] **SC-3.4** Open a new terminal, run `claude`, then close the terminal tab while it's running. Verify immediate AGENT_ACTIVE → CODING/IDLE (no grace — onDidCloseTerminal supersedes).
    - [ ] **SC-3.5** Open two integrated terminals. Run `claude` in both. Verify both sessions tracked. Quit one (Ctrl+C). Verify Discord STAYS AGENT_ACTIVE (the second is still running). Quit the second. Verify Discord downgrades after the 30 s grace of the second session.

    ## Checklist 2 — JSONL fs-watch fallback (DET-05)

    - [ ] **SC-3.6** Disable shell integration (e.g. `unsetopt PROMPT_SUBST` in zsh, or use a shell config without VS Code shell-integration script). Run `claude`. Verify Discord still flips to AGENT_ACTIVE within ~5 s via the JSONL fs-watch tier.
    - [ ] **SC-3.7** (Linux only — best-effort) Repeat SC-3.6 on a Linux Dev Host. Verify the polling-stat fallback picks up the session (recursive fs.watch is unsupported on Linux).

    ## Checklist 3 — Other agents (DET-03)

    - [ ] **SC-3.8** If aider is installed (`pip install aider-chat`), run `python -m aider` in a terminal. Verify Discord flips to AGENT_ACTIVE with agent=`aider`.

    ## Sign-off

    Phase 03 manual sign-off requires SC-3.1, SC-3.2, SC-3.3, SC-3.4 minimum. SC-3.5..SC-3.8 are nice-to-have.
    ```
  </action>
  <verify>
    <automated>pnpm check:api-surface &amp;&amp; test -f .planning/phases/03-agent-detection/03-HUMAN-UAT.md</automated>
  </verify>
  <done>
    pnpm check:api-surface PASS message lists the new pure-core path count (4 instead of 3); 03-HUMAN-UAT.md exists with all 8 SC-3.* items.
  </done>
</task>

</tasks>

<verification>
- pnpm typecheck PASS
- pnpm test PASS (Phase 1+2 still green; new tests appear as todos)
- pnpm check:api-surface PASS
- pnpm check:bundle-size PASS (no src/ change yet)
- All 5 new test files contain only `it.todo(...)` — grep `it\.todo` count matches the spec above
- 03-HUMAN-UAT.md exists with SC-3.1..SC-3.8
</verification>

<pitfalls>
- Do NOT add implementation in test files — Wave 1 owns flipping todos. Adding real assertions here couples 03-00 to source modules that don't yet exist.
- Do NOT add `src/detectors/` (broad) to PURE_CORE_PATHS — only `src/detectors/regex.ts`. The other detector files MUST import vscode (they're adapters).
- The fakeTerminal helper is intentionally typed `as unknown as vscode.Terminal` — the real type has dozens of fields we won't mock. This is a test fixture, not production code.
- Compile-only: tests must `import { describe, it } from "vitest"` but must NOT yet import source modules that don't exist (`src/detectors/shellIntegration.ts` etc.). Wave 1 adds those imports when flipping todos.
</pitfalls>

<success_criteria>
Phase 3 Wave 0 is complete when:
- Test scaffolding for all 5 detector modules exists with full coverage of DET-01..10 as it.todo entries
- Fixture helpers exist with the locked interface signatures
- PURE_CORE guard extended for src/detectors/regex.ts
- 03-HUMAN-UAT.md authored
- All five guardrails pass (test, typecheck, build, check:api-surface, check:bundle-size)
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-detection/03-00-SUMMARY.md` per template.
</output>
