---
phase: 05-companion-plugin-oss-hygiene-assets-readme
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/ISSUE_TEMPLATE/bug_report.md
  - .github/ISSUE_TEMPLATE/feature_request.md
  - .github/PULL_REQUEST_TEMPLATE.md
autonomous: true
requirements:
  - DIST-03
  - DIST-04
  - DIST-05

must_haves:
  truths:
    - "Bug report template includes all structured fields: VS Code version, Cursor version, Discord version, OS, agent CLI, shell, steps to reproduce, debug.verbose log capture"
    - "Feature request template includes: problem, proposed solution, persona"
    - "PR template includes checklist: tests pass, no new runtime deps, PRD guardrails"
  artifacts:
    - path: ".github/ISSUE_TEMPLATE/bug_report.md"
      provides: "Structured bug report template"
      contains: "debug.verbose"
    - path: ".github/ISSUE_TEMPLATE/feature_request.md"
      provides: "Structured feature request template"
      contains: "Persona"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "PR checklist template"
      contains: "tests pass"
  key_links: []
---

<objective>
Create GitHub issue templates (bug report + feature request) and a PR template with structured fields.

Purpose: Standardize bug reports with the diagnostic fields needed for this extension (Discord version, agent CLI, shell type) and enforce PR quality via a checklist. Required by DIST-03, DIST-04, DIST-05.

Output: Three template files in `.github/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-companion-plugin-oss-hygiene-assets-readme/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create issue templates</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.md, .github/ISSUE_TEMPLATE/feature_request.md</files>
  <action>
Create `.github/ISSUE_TEMPLATE/bug_report.md` (per DIST-03):

Use GitHub issue template markdown format with YAML frontmatter:
```yaml
---
name: Bug Report
about: Report a bug with the Agent Mode Discord extension
title: "[Bug] "
labels: bug
assignees: ""
---
```

Body with structured sections:
- **Environment** (all fields required per DIST-03):
  - VS Code version: (e.g., 1.93.0)
  - Cursor version: (if applicable)
  - Discord version: (e.g., Stable 0.0.xxx)
  - OS: (e.g., macOS 15.0, Windows 11, Ubuntu 24.04)
  - Agent CLI used: (e.g., claude, aider, codex, gemini, opencode)
  - Shell: (e.g., bash, zsh, fish, PowerShell, Command Prompt)
- **Description**: What happened?
- **Steps to Reproduce**: Numbered list
- **Expected Behavior**: What should have happened
- **Actual Behavior**: What actually happened
- **Debug Logs**: "Set `agentMode.debug.verbose: true` in VS Code settings, reproduce the issue, then paste the 'Agent Mode (Discord)' output channel contents below:"
  - Fenced code block placeholder

Create `.github/ISSUE_TEMPLATE/feature_request.md` (per DIST-04):

YAML frontmatter:
```yaml
---
name: Feature Request
about: Suggest a feature for Agent Mode Discord
title: "[Feature] "
labels: enhancement
assignees: ""
---
```

Body with structured sections:
- **Problem**: What problem does this solve?
- **Proposed Solution**: How should it work?
- **Persona**: Which user persona does this serve?
  - Marcus (power user, terminal-centric, multi-agent)
  - Steph (casual, primarily Claude, wants it to "just work")
  - Other (describe)
- **Alternatives Considered**: What else could solve this?
- **Additional Context**: Screenshots, links, etc.
  </action>
  <verify>
    <automated>cd /Users/leonardojaques/projects/personal/richagenticpresence-discord && test -f .github/ISSUE_TEMPLATE/bug_report.md && test -f .github/ISSUE_TEMPLATE/feature_request.md && grep -q "debug.verbose" .github/ISSUE_TEMPLATE/bug_report.md && grep -q "Persona" .github/ISSUE_TEMPLATE/feature_request.md && echo "PASS: issue templates valid"</automated>
  </verify>
  <done>Bug report template has all DIST-03 structured fields including debug.verbose log capture; feature request template has problem/solution/persona fields per DIST-04</done>
</task>

<task type="auto">
  <name>Task 2: Create PR template</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md</files>
  <action>
Create `.github/PULL_REQUEST_TEMPLATE.md` (per DIST-05):

Structure:
- **Summary**: Brief description of what this PR does
- **Related Issue**: Fixes #(issue number) — required per CONTRIBUTING.md "file issue before large PRs"
- **Type of Change**: Checkbox list (bug fix / new feature / breaking change / docs / refactor)
- **Checklist** (per DIST-05):
  - [ ] Tests pass (`pnpm test`)
  - [ ] Lint passes (`pnpm lint`)
  - [ ] Build succeeds (`pnpm build`)
  - [ ] Bundle size is within 500 KB budget (`pnpm check:bundle-size`)
  - [ ] No new runtime dependencies added
  - [ ] No VS Code proposed APIs or `(vscode as any).*` casts
  - [ ] Follows Conventional Commits format
  - [ ] Files over 200 lines are split along natural boundaries
- **Screenshots/GIFs** (if applicable)
  </action>
  <verify>
    <automated>cd /Users/leonardojaques/projects/personal/richagenticpresence-discord && test -f .github/PULL_REQUEST_TEMPLATE.md && grep -q "pnpm test" .github/PULL_REQUEST_TEMPLATE.md && grep -q "runtime dependencies" .github/PULL_REQUEST_TEMPLATE.md && echo "PASS: PR template valid"</automated>
  </verify>
  <done>PR template has summary, related issue, type of change, and full checklist covering tests, lint, build, bundle size, no new deps, no proposed APIs, Conventional Commits, file size</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Static markdown templates with no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | I (Information Disclosure) | bug_report.md | mitigate | Template asks for debug.verbose logs which may contain workspace paths; add note "redact any sensitive paths before posting" |
</threat_model>

<verification>
1. Bug report template has all 8 environment fields from DIST-03
2. Feature request template has problem/solution/persona fields from DIST-04
3. PR template has full checklist from DIST-05
4. All files are in correct `.github/` locations
</verification>

<success_criteria>
- Three GitHub templates exist in correct locations
- Bug report captures all diagnostic info needed for this extension
- PR checklist enforces project quality standards
</success_criteria>

<output>
After completion, create `.planning/phases/05-companion-plugin-oss-hygiene-assets-readme/05-04-SUMMARY.md`
</output>
