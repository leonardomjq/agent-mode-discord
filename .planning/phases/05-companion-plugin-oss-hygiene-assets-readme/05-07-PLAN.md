---
phase: 05-companion-plugin-oss-hygiene-assets-readme
plan: 07
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
  - 05-05
  - 05-06
autonomous: true
files_modified:
  - README.md
  - package.json
autonomous: true
requirements:
  - DIST-09
  - DIST-07

must_haves:
  truths:
    - "README has all 13 sections in the order specified by D-12"
    - "Demo GIF is above the fold (section 2)"
    - "Install sections cover Marketplace, OpenVSX, and manual VSIX"
    - "Goblin pack preview shows code block with pool examples"
    - "Privacy FAQ explains what leaks by default"
    - "Competitive positioning table compares vs vscord, discord-vscode, RikoAppDev"
    - "Troubleshooting covers Cursor-on-Windows, fish, Command Prompt, Flatpak Discord"
    - "Maintainer-posture line matches D-13 tone"
    - "package.json repository URL updated to leonardojaques/agent-mode-discord (D-14)"
  artifacts:
    - path: "README.md"
      provides: "Portfolio-grade README with all 13 sections"
      min_lines: 200
    - path: "package.json"
      provides: "Updated repository URL"
      contains: "leonardojaques/agent-mode-discord"
  key_links:
    - from: "README.md"
      to: "assets/demo.gif"
      via: "Markdown image embed"
      pattern: "demo\\.gif"
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "Markdown link"
      pattern: "CONTRIBUTING"
    - from: "README.md"
      to: "LICENSE"
      via: "License section reference"
      pattern: "MIT"
---

<objective>
Create the portfolio-grade README with all 13 sections per D-12, and update `package.json` repository URL per D-14.

Purpose: The README is the storefront for the extension — it's what users see on the Marketplace, GitHub, and OpenVSX. Must communicate value immediately (demo GIF above the fold), provide clear install paths, and differentiate from competitors. DIST-09 specifies the exact section order.

Output: Complete `README.md` at repo root, updated `package.json` repository URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-companion-plugin-oss-hygiene-assets-readme/05-CONTEXT.md
@.planning/phases/05-companion-plugin-oss-hygiene-assets-readme/05-RESEARCH.md

<!-- Prior plan summaries needed for content references -->
@.planning/phases/05-companion-plugin-oss-hygiene-assets-readme/05-01-SUMMARY.md
@.planning/phases/05-companion-plugin-oss-hygiene-assets-readme/05-02-SUMMARY.md
@.planning/phases/05-companion-plugin-oss-hygiene-assets-readme/05-03-SUMMARY.md

<interfaces>
<!-- package.json current state for repository URL update -->
From package.json:
```json
{
  "name": "agent-mode-discord",
  "displayName": "Agent Mode",
  "publisher": "agent-mode-dev",
  "repository": { "type": "git", "url": "https://github.com/TODO/agent-mode-discord.git" }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json repository URL</name>
  <files>package.json</files>
  <action>
Update the `repository` field in `package.json` (per D-14):

Change from:
```json
"repository": { "type": "git", "url": "https://github.com/TODO/agent-mode-discord.git" }
```
To:
```json
"repository": { "type": "git", "url": "https://github.com/leonardojaques/agent-mode-discord.git" }
```

Do NOT change any other fields in package.json. Read the file first, make only this one change.
  </action>
  <verify>
    <automated>cd /Users/leonardojaques/projects/personal/richagenticpresence-discord && node -e "const p=require('./package.json'); if(!p.repository.url.includes('leonardojaques')) process.exit(1); console.log('PASS: repo URL updated')"</automated>
  </verify>
  <done>package.json repository URL points to leonardojaques/agent-mode-discord</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md with all 13 sections</name>
  <files>README.md</files>
  <action>
Create `README.md` at repo root following D-12's exact 13-section order. The README is the extension's storefront — lead with the core value ("stop looking AFK during AI sessions"), not generic feature lists.

**Section 1: Tagline + badges**
- H1: "Agent Mode"
- One-sentence tagline: "Discord Rich Presence that knows when you're cooking with Claude — not idling."
- Badges (use pre-publish safe badges per RESEARCH Pitfall 7):
  - `[![CI](https://github.com/leonardojaques/agent-mode-discord/actions/workflows/ci.yml/badge.svg)](https://github.com/leonardojaques/agent-mode-discord/actions/workflows/ci.yml)`
  - `[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)`
  - Leave Marketplace/install badges commented out with `<!-- Uncomment after Phase 6 publish -->` note

**Section 2: Demo GIF (above the fold)**
- `![Agent Mode Demo](assets/demo.gif)`
- If demo.gif doesn't exist yet (plan 05-06 may have been skipped), use: `<!-- Demo GIF: see assets/CAPTURE-INSTRUCTIONS.md -->`
- Caption: "> Your Discord friends see 'cooking with Claude' instead of a stale away dot."

**Section 3: Features (short bullet list)**
- Real-time agent detection — flips to "cooking" when `claude` runs in the terminal
- Multi-tier detection — Shell Integration > session file watch > polling (works even when shell integration is unavailable)
- Companion plugin — highest-fidelity detection via lockfile signal
- Goblin personality — rotating goblin-voice copy with frame animations
- Privacy controls — show/hide/hash workspace, filename, branch; ignore lists for repos/orgs
- Zero network — Discord IPC only, no HTTP requests, no telemetry
- Lightweight — under 500 KB, activates in under 50ms

**Section 4: Install**
Three sub-sections:

**VS Code Marketplace:**
```
ext install agent-mode-dev.agent-mode-discord
```
Or search "Agent Mode" in the Extensions sidebar.

**OpenVSX (Cursor / VSCodium / Windsurf):**
```
ext install agent-mode-dev.agent-mode-discord
```
Or download from [open-vsx.org](https://open-vsx.org/extension/agent-mode-dev/agent-mode-discord).

**Manual VSIX:**
```bash
# Download the latest .vsix from GitHub Releases
code --install-extension agent-mode-discord-0.1.0.vsix
```

**Companion Plugin (optional, recommended):**
```bash
claude plugin install ./companion/claude-code-plugin
```
Provides the highest-fidelity detection signal. See [companion/claude-code-plugin/README.md](companion/claude-code-plugin/README.md).

**Section 5: Goblin pack preview**
- Brief intro: "Agent Mode ships with the `goblin` personality pack. Messages rotate every 20 seconds with frame animations."
- Code block showing a sample of the goblin.json pools:
  ```
  AGENT_ACTIVE (claude): "the goblin is consulting claude on dungeon logistics"
  AGENT_ACTIVE (generic): "a creature is receiving instructions from the abyss"
  CODING: "the goblin is scribbling runes into the editor"
  IDLE: "the goblin waits... tapping its claws on the desk..."
  ```
- Note: "Point `agentMode.messages.customPackPath` at your own JSON file to use a custom pack."

**Section 6: Configuration**
- Brief intro: "Agent Mode exposes ≤20 settings under `agentMode.*`. All changes apply on the next rotation tick (no reload required)."
- Link to VS Code settings: `Preferences: Open Settings (UI)` → search "Agent Mode"
- Key settings table:

| Setting | Default | Description |
|---------|---------|-------------|
| `agentMode.clientId` | (bundled) | Override Discord Client ID |
| `agentMode.idleBehavior` | `show` | `show` or `clear` on idle |
| `agentMode.privacy.workspaceName` | `show` | `show` / `hide` / `hash` |
| `agentMode.privacy.filename` | `show` | `show` / `hide` |
| `agentMode.privacy.gitBranch` | `show` | `show` / `hide` |
| `agentMode.animations.enabled` | `true` | Enable frame cycling |
| `agentMode.debug.verbose` | `false` | Verbose output channel logging |

**Section 7: Privacy FAQ**
- "**What does this extension send to the internet?** Nothing. It communicates exclusively over Discord's local IPC socket (Unix socket on macOS/Linux, named pipe on Windows). Zero outbound HTTP requests. Verified by a CI check that greps the built bundle for HTTP patterns."
- "**What shows in Discord by default?** Your workspace name, active filename, git branch, and goblin-voice status copy. All visible to your Discord friends."
- "**How do I hide sensitive info?** Set `agentMode.privacy.workspaceName` to `hide` or `hash`, `agentMode.privacy.filename` to `hide`, `agentMode.privacy.gitBranch` to `hide`. Use `agentMode.ignore.workspaces` glob patterns to fully silence the extension in specific workspaces."
- "**Can my employer see my activity?** Only your Discord friends can see your Rich Presence. There is no server-side component, no analytics, no telemetry."

**Section 8: Competitive positioning table**
- Intro: "How Agent Mode compares to existing Discord presence extensions (as of v0.1.0):"

| Feature | Agent Mode | vscord | discord-vscode | RikoAppDev |
|---------|-----------|--------|----------------|------------|
| Terminal agent detection | Yes (multi-tier) | No | No | Partial (proposed API) |
| Shell Integration API | Yes (stable API) | No | No | No (uses `(vscode as any).chat`) |
| Claude Code companion | Yes (lockfile) | No | No | No |
| Multi-agent support | Yes (5 agents) | No | No | Partial (edit heuristic) |
| Marketplace-compliant | Yes (zero proposed APIs) | Yes | Yes | No (`(vscode as any).*`) |
| Bundle size | <500 KB | ~2 MB | ~1 MB | ~500 KB |
| Configuration keys | ≤20 | 160+ | ~30 | ~15 |
| Privacy controls | show/hide/hash + ignore lists | Limited | Basic | None |
| Network requests | None (IPC only) | None | None | Unknown |
| Copy personality | Goblin pack + custom packs | Generic | Generic | Generic |

Per T-05-04: state "as of v0.1.0, April 2026" and link to each extension's Marketplace page for verification.

**Section 9: Troubleshooting**
- **Cursor on Windows**: Shell Integration may be unreliable. Agent Mode falls back to `~/.claude/projects/*.jsonl` file watching (tier-3). For best results, install the companion plugin.
- **fish shell**: Shell Integration works with fish. If detection fails, check that fish's VS Code integration script is loaded (`string match -q "$TERM_PROGRAM" vscode`).
- **Command Prompt (cmd.exe)**: Shell Integration is not available in cmd.exe. Use PowerShell or install the companion plugin for agent detection.
- **Flatpak Discord**: Discord installed via Flatpak may not expose the IPC socket to other applications. Use the `.deb`/`.tar.gz` install or configure Flatpak to share the IPC socket (`flatpak override --user --filesystem=xdg-run/discord-ipc-*`).
- **"No presence showing"**: Ensure Discord desktop is running. Check Activity Privacy settings in Discord (Settings > Activity Privacy > "Display current activity as a status message" must be ON). Enable `agentMode.debug.verbose` and check the "Agent Mode (Discord)" output channel.

**Section 10: Contributing**
- "Contributions welcome! Please read [CONTRIBUTING.md](CONTRIBUTING.md) before opening a PR."
- "File an issue first for anything beyond a typo fix."

**Section 11: Sponsor placeholder**
- `<!-- Sponsor placeholder: activated if install count crosses 500 (DIST-V2-01) -->`
- Visible text: "If this extension saves you from looking AFK during your AI sessions, consider starring the repo."

**Section 12: MIT license line**
- "[MIT](LICENSE) -- 2026 Leonardo Jaques"

**Section 13: Maintainer-posture line (per D-13)**
- "Solo project, maintained on my own schedule. Issues welcome; PRs require a filed issue first. Response time varies -- this is a passion project, not a product."
  </action>
  <verify>
    <automated>cd /Users/leonardojaques/projects/personal/richagenticpresence-discord && test -f README.md && grep -q "Agent Mode" README.md && grep -q "demo.gif\|Demo GIF" README.md && grep -q "Marketplace" README.md && grep -q "OpenVSX" README.md && grep -q "goblin" README.md && grep -q "Privacy" README.md && grep -q "vscord" README.md && grep -q "Troubleshooting\|troubleshooting" README.md && grep -q "Cursor.*Windows\|Cursor-on-Windows" README.md && grep -q "passion project" README.md && grep -q "MIT" README.md && echo "PASS: README has all 13 sections"</automated>
  </verify>
  <done>README.md at repo root with all 13 sections per D-12 in exact order: tagline+badges, demo GIF, features, install (3 paths), goblin preview, config table, privacy FAQ, competitive table, troubleshooting (4 cases), contributing link, sponsor placeholder, MIT line, maintainer-posture line matching D-13</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README content → public readers | Competitive claims must be verifiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | R (Repudiation) | README.md | mitigate | Competitive positioning table states "as of v0.1.0, April 2026" with links to each extension's Marketplace page for verification |
| T-05-09 | I (Information Disclosure) | README.md | accept | README documents what data is shown in Discord by default; privacy FAQ explains controls |
</threat_model>

<verification>
1. README.md exists with all 13 sections in D-12 order
2. `package.json` repository URL points to `leonardojaques/agent-mode-discord`
3. Demo GIF reference is present (either actual image or placeholder)
4. All external links are formatted correctly
5. Competitive table has all 4 extensions compared
6. Troubleshooting covers all 4 cases (Cursor-on-Windows, fish, Command Prompt, Flatpak Discord)
7. Maintainer-posture matches D-13 tone verbatim
</verification>

<success_criteria>
- README is portfolio-grade with clear visual hierarchy
- Value proposition ("stop looking AFK during AI sessions") is above the fold
- All install paths documented (Marketplace, OpenVSX, VSIX, companion plugin)
- Privacy FAQ answers "what leaks by default" clearly
- Competitive table is factual and verifiable
- D-13 maintainer posture sets honest expectations
- D-14 repository URL updated in package.json
</success_criteria>

<output>
After completion, create `.planning/phases/05-companion-plugin-oss-hygiene-assets-readme/05-07-SUMMARY.md`
</output>
