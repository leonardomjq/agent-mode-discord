---
phase: 05.2-multi-window-leadership-election
plan: 02
type: execute
wave: 2
depends_on: [05.2-01]
files_modified:
  - src/extension.ts
autonomous: true
requirements: []
# No new REQ IDs — polish/architecture. Satisfies PRD §4 multi-window expectations
# via the single-leader semantics defined in 05.2-CONTEXT.md (D-05, D-06, D-10, D-13).
covers_decisions:
  - D-05  # followers do NOT instantiate ConnectionManager; poll for takeover every 30s
  - D-06  # on takeover, follower starts fresh (IDLE) — no state replay
  - D-10  # leadership.acquire() called BEFORE mgr.start() / activityBuilder.start()
  - D-13  # debug.verbose logging: acquired / released / taken-over

must_haves:
  truths:
    - "When the first VS Code window with the extension starts, it acquires leadership and the full Discord RPC flow (mgr.start + activityBuilder.start + detectors) runs as today"
    - "When a second VS Code window starts while the leader is alive, it does NOT instantiate ConnectionManager, does NOT start activityBuilder, and does NOT register detectors"
    - "When the leader process exits gracefully, its deactivate() calls leadership.release() which unlinks the lockfile"
    - "When the leader crashes (no deactivate runs), a follower polling every 30s observes the stale lockfile within 90s and takes over by lazily bootstrapping the same driver flow"
    - "After takeover, the new leader's presence starts from IDLE (no shared state from the previous leader — D-06)"
    - "On extension-host dispose, leadership.release() is awaited alongside the existing mgr.stop() teardown"
  artifacts:
    - path: "src/extension.ts"
      provides: "Leader/follower split in createDriver; leadership.acquire() before mgr.start(); onTakeover lazy-bootstrap path; leadership.release() in dispose"
      contains: "createLeadership"
  key_links:
    - from: "src/extension.ts"
      to: "src/state/leadership.ts"
      via: "import { createLeadership } from \"./state/leadership\""
      pattern: "createLeadership"
    - from: "src/extension.ts activate() path"
      to: "mgr.start() / activityBuilder.start() / detector registrations"
      via: "conditional on isLeader — guarded behind leadership.acquire() or onTakeover callback"
      pattern: "if.*isLeader|onTakeover"
---

<objective>
Wire `createLeadership` from Plan 05.2-01 into `src/extension.ts` so the first-opened VS Code window owns Discord presence and subsequent windows stay silent followers — with a lazy-bootstrap takeover path if the leader dies.

Purpose: Deliver the user-visible behavior of Phase 05.2 ("show the first one open, simple"). The underlying leadership primitive is already shipped by 05.2-01; this plan makes the extension host actually use it.

Output: A modified `src/extension.ts` in which `createDriver()` splits into a leader path (today's full flow) and a follower path (idle poll for takeover). On takeover, the follower lazily instantiates the full driver. `deactivate()` cleanly releases the lockfile if this window was leader.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-multi-window-leadership-election/05.2-CONTEXT.md
@.planning/phases/05.2-multi-window-leadership-election/05.2-01-PLAN.md

# Code being modified (read ALL before writing)
@src/extension.ts
@src/state/leadership.ts

<interfaces>
<!-- Exports this plan depends on (delivered by 05.2-01). Executor does NOT need to explore leadership.ts — full contract is here. -->

From src/state/leadership.ts (exported by Plan 05.2-01):
```typescript
export interface Leadership {
  acquire(): Promise<boolean>;     // true = this window is now leader; false = another window holds it fresh
  release(): Promise<void>;        // unlinks the lockfile if this window is leader; silent on failure (D-07)
  isLeader(): boolean;
  onTakeover(cb: () => void): void; // cb fires on successful stale-takeover acquire()
}

export function createLeadership(opts?: LeadershipOptions): Leadership;
```

Existing signatures in src/extension.ts (do not change, only wrap/gate):
- `createConnectionManager(clientId, pid, realBackoffDeps) → ConnectionManager`  (line ~70)
- `createActivityBuilder({ ... }) → { start(), stop(), forceTick() }`  (line ~96)
- `createEditorDetector(dispatch) → Disposable`  (line ~181)
- `createGitDetector(dispatch) → Disposable`  (line ~182)
- `createDetectorsOrchestrator(dispatch, opts) → Disposable`  (line ~183)
- `registerSignalHandlers(client, pid) → () => void`  (imported from ./rpc/client)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split createDriver into leader and follower paths</name>
  <read_first>
    - src/extension.ts (full file — current createDriver implementation at lines 35-207)
    - src/state/leadership.ts (contract this plan consumes)
    - .planning/phases/05.2-multi-window-leadership-election/05.2-CONTEXT.md (D-05, D-06, D-10, D-13)
  </read_first>
  <behavior>
    After this task, src/extension.ts should behave as follows:

    1. On `activate()`, `createDriver()` is still called and a top-level `driver` is still stored (no change to module-level shape).

    2. Inside `createDriver()`:
       a. Create `const leadership = createLeadership({ log: (m) => log(m, { verboseOnly: true }) });` per D-13 (log sink is the existing outputChannel log gated on debug.verbose).
       b. Keep the existing `bootCfg` read and `setVerboseCache(bootCfg.debug.verbose)` call as the first two lines — they need to run BEFORE acquire() so the optional log callback inside leadership works.
       c. Kick off `leadership.acquire()` as a Promise. Use an internal `bootstrapAsLeader()` closure that contains ALL of the current leader-path setup (mgr, throttledSet, onSet/onClear, getPack, activityBuilder, scheduleIdle, dispatch, mgr.onReady, configListener, detectors, mgr.start(), activityBuilder.start()). Convert the existing inline code into that closure.
       d. When `leadership.acquire()` resolves to `true`: call `bootstrapAsLeader()`. Log "[leadership] acquired — bootstrapping as leader" via `log(..., { verboseOnly: true })` (D-13).
       e. When `leadership.acquire()` resolves to `false`: log "[leadership] held by another window — this window is a follower" via verbose log (D-13). Register `leadership.onTakeover(() => bootstrapAsLeader())` so if the follower is promoted later, it bootstraps the full driver. Do NOT run any detector registration, do NOT start mgr / activityBuilder (D-05).
       f. `bootstrapAsLeader()` must be idempotent: track a `bootstrapped: boolean` flag and return early if already true (guards against double-fire if acquire() races with onTakeover in a pathological case). Set bootstrapped = true on entry.

    3. The returned `Driver.dispose` (the `dispose: async () => { ... }` at the bottom of createDriver) must:
       a. Always await `leadership.release()` regardless of whether this window bootstrapped (safe no-op when not leader — release() early-returns if !isLeader).
       b. Only run the existing teardown (activityBuilder.stop, configListener.dispose, detector disposables, unregisterSignals, mgr.stop) if `bootstrapped` is true. Wrap existing disposables so that their variables (mgr, configListener, editorDisposable, gitDisposable, detectorsDisposable, activityBuilder) are hoisted to be accessible in dispose but only populated during bootstrapAsLeader. Initialize them to `undefined` at the top of createDriver; the dispose closure should guard each `try { X?.dispose(); } catch { /* silent */ }`.

    4. D-06 compliance (follower takeover starts fresh): Because bootstrapAsLeader creates a fresh `state = initialState()` local and a fresh `mgr`/`activityBuilder`, no state is carried from the previous leader process (which lived in a different OS process anyway). No additional code required — just verify by inspection that `state`, `idleTimer`, `unregisterSignals` are all local to bootstrapAsLeader and NOT captured from an earlier closure.

    5. Zero new runtime deps. Only add one import: `import { createLeadership } from "./state/leadership";`.

    6. `activate()` must NOT become async. `createDriver()` keeps its synchronous signature — the acquire() promise is handled internally via `.then(...)` (do NOT await in activate/createDriver; Phase-4 contract is activate runs in <50ms and VS Code already handles async bootstrapping elsewhere).

    CONSTRAINTS:
    - Do NOT change any file other than src/extension.ts.
    - Do NOT remove any existing behavior from the leader path (everything that runs today must still run when this window is leader).
    - Do NOT add new verbose-gate checks — reuse the existing `log(..., { verboseOnly: true })` pattern.
    - File size: src/extension.ts is currently ~207 lines; adding ~30-40 lines pushes it close to the 200-line PROJECT.md guideline. If the file crosses 220 lines post-edit, extract bootstrapAsLeader into a helper module `src/leaderDriver.ts` — but try to stay inline first (the refactor blast radius is smaller).
  </behavior>
  <action>
    1. Read `src/extension.ts` end-to-end to map the exact lines that become the bootstrapAsLeader closure body.
    2. Edit `src/extension.ts` to introduce `createLeadership` import and the leader/follower split described in &lt;behavior&gt;. Follow these specific steps:
       - Add `import { createLeadership } from "./state/leadership";` in the import block.
       - Inside `createDriver`, immediately after the `setVerboseCache(...)` try/catch, instantiate `const leadership = createLeadership({ log: (m) => log(m, { verboseOnly: true }) });`.
       - Hoist variable declarations that dispose() needs to access: `let mgr: ConnectionManager | undefined; let activityBuilder: { start: () => void; stop: () => void; forceTick: () => void } | undefined; let configListener: vscode.Disposable | undefined; let editorDisposable: vscode.Disposable | undefined; let gitDisposable: vscode.Disposable | undefined; let detectorsDisposable: vscode.Disposable | undefined; let unregisterSignals: (() => void) | undefined; let idleTimer: NodeJS.Timeout | null = null; let state: State = initialState(); let bootstrapped = false;`
       - Move the existing driver setup (mgr assignment, throttledSet, onSet, onClear, getPack, activityBuilder assignment, scheduleIdle, dispatch, mgr.onReady handler, configListener assignment, editorDisposable / gitDisposable / detectorsDisposable assignments, mgr.start(), activityBuilder.start()) into a `function bootstrapAsLeader(): void { if (bootstrapped) return; bootstrapped = true; /* existing body */ }` declared inside createDriver.
       - Wire acquire():
         ```typescript
         void leadership.acquire().then((isLeader) => {
           if (shuttingDown) return;
           if (isLeader) {
             log("[leadership] acquired — bootstrapping as leader", { verboseOnly: true });
             bootstrapAsLeader();
           } else {
             log("[leadership] held by another window — this window is a follower", { verboseOnly: true });
             leadership.onTakeover(() => {
               if (shuttingDown) return;
               log("[leadership] taken over from stale leader — bootstrapping", { verboseOnly: true });
               bootstrapAsLeader();
             });
           }
         }).catch(() => { /* silent per D-18 */ });
         ```
       - Update the returned `Driver.dispose` to (a) await `leadership.release()` first, (b) keep the rest of the teardown but with `?.` optional-chain on the hoisted variables since they may be undefined (follower case).
    3. Run `pnpm typecheck` and iterate until clean.
    4. Run `pnpm test` and iterate until green. No test in the existing suite should be touched — they all mock or bypass the extension.ts activate flow. If one does break, investigate; most likely it's a tsc error that bled into a test.
    5. Run `pnpm build` — must produce dist/extension.cjs under 500 KB with no warnings.

    Commit: `feat(05.2-02): wire leadership election into extension.ts activate/deactivate`
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm test --run &amp;&amp; pnpm build &amp;&amp; test $(stat -f%z dist/extension.cjs 2>/dev/null || stat -c%s dist/extension.cjs) -lt 512000</automated>
  </verify>
  <done>
    - `src/extension.ts` imports `createLeadership` from `./state/leadership`
    - `createDriver` contains a `bootstrapAsLeader()` helper gated by a `bootstrapped` flag
    - `leadership.acquire()` is called before any `mgr.start()` / `activityBuilder.start()` (D-10)
    - Follower path registers `leadership.onTakeover(...)` and returns without starting RPC (D-05)
    - `Driver.dispose` awaits `leadership.release()` and guards teardown with optional chaining
    - `pnpm typecheck`, `pnpm test --run`, `pnpm build` all pass
    - Bundle size under 500 KB
    - Commit created with message prefix `feat(05.2-02):`
  </done>
  <acceptance_criteria>
    - `grep -q "import { createLeadership } from \"./state/leadership\"" src/extension.ts` exits 0
    - `grep -q "leadership.acquire()" src/extension.ts` exits 0
    - `grep -q "leadership.onTakeover" src/extension.ts` exits 0
    - `grep -q "leadership.release()" src/extension.ts` exits 0
    - `grep -q "bootstrapAsLeader" src/extension.ts` exits 0
    - `grep -q "bootstrapped" src/extension.ts` exits 0
    - acquire()-before-mgr.start() ordering holds — verify with: `awk '/leadership.acquire/{a=NR} /mgr\\.start\\(\\)/{if (a==0 || NR>a) print "OK"; else print "BAD: mgr.start before acquire"; exit}' src/extension.ts` prints `OK` or produces no "BAD" output
    - `pnpm typecheck` exits 0
    - `pnpm test --run` exits 0 (full suite green)
    - `pnpm build 2>&1 | grep -qiE "error|warning: "` exits NON-zero (no build warnings)
    - `test -f dist/extension.cjs` exits 0
    - Bundle size (bytes of `dist/extension.cjs`) strictly less than 512000
    - `git log -1 --oneline | grep -q "feat(05.2-02)"` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify dist bundle contents + behavior invariants</name>
  <read_first>
    - src/extension.ts (just-edited file)
    - scripts/check-no-network.mjs
    - scripts/check-api-surface.mjs (zero proposed APIs / zero `(vscode as any).*` casts)
    - scripts/check-bundle-size.mjs
  </read_first>
  <action>
    Run the existing CI guardrails against the built bundle to confirm this change didn't regress any Marketplace invariants:

    1. `pnpm build` — confirm dist/extension.cjs exists and is under 500 KB.
    2. `node scripts/check-no-network.mjs` — leadership wiring must not introduce any HTTP patterns.
    3. `node scripts/check-api-surface.mjs` — no new proposed APIs, no `(vscode as any).*` casts introduced.
    4. `node scripts/check-bundle-size.mjs` — bundle budget still passes.
    5. `pnpm test` full suite — total count matches pre-plan count + new leadership tests from 05.2-01 (no regressions).
    6. `grep -c "leadership" dist/extension.cjs` should return a positive number (sanity: our new code actually made it into the bundle — not tree-shaken).

    If ANY guardrail fails, diagnose and fix (most likely a tsc strictness issue, a missed import, or an accidentally-added proposed API reference).

    Do NOT create an additional commit if no code changes — this task is a verification gate. If a fix is required, commit as `fix(05.2-02): [specific fix]`.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; node scripts/check-no-network.mjs &amp;&amp; node scripts/check-api-surface.mjs &amp;&amp; node scripts/check-bundle-size.mjs &amp;&amp; pnpm test --run</automated>
  </verify>
  <done>
    - All four CI guardrail scripts exit 0
    - `pnpm test --run` exits 0
    - Bundle contains the leadership code (grep for identifier `leadership` in dist/extension.cjs returns >0 matches)
  </done>
  <acceptance_criteria>
    - `pnpm build` exits 0
    - `node scripts/check-no-network.mjs` exits 0
    - `node scripts/check-api-surface.mjs` exits 0
    - `node scripts/check-bundle-size.mjs` exits 0
    - `pnpm test --run` exits 0
    - `grep -c "leadership" dist/extension.cjs` returns a number &gt; 0 (code reached the bundle)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension process (leader) → Discord IPC | Unchanged from pre-05.2: Discord desktop local socket; pid-scoped setActivity. |
| extension process (follower) → filesystem (`~/.claude/`) | Follower only polls the lockfile mtime (via Plan 05.2-01's leadership module). No Discord IPC opened from follower processes. This SHRINKS the attack surface compared to pre-05.2 behavior (previously every window opened its own IPC). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-05 | Denial of Service | extension.ts leader/follower split | mitigate | bootstrapAsLeader is idempotent via `bootstrapped` flag; acquire() and onTakeover paths cannot double-instantiate mgr or activityBuilder. `shuttingDown` flag is checked at every async boundary (acquire .then, onTakeover cb) to prevent zombie bootstraps after deactivate. |
| T-05.2-06 | Information Disclosure | verbose leadership logs | accept | Log lines are gated behind `debug.verbose` per D-13 — off by default. Content is "acquired" / "held by another window" / "taken over" — no paths, no PII. |

No new network surface. `scripts/check-no-network.mjs` enforced in Task 2.
</threat_model>

<verification>
After this plan completes:
1. Launch two VS Code windows in sequence. First window shows Discord presence; second window does not (verifiable by observing Discord — outside automated scope, covered by Plan 05.2-03's HUMAN-UAT update).
2. Automated check: `grep -q "leadership.acquire" src/extension.ts && grep -q "bootstrapAsLeader" src/extension.ts` exits 0.
3. `pnpm test` full suite green.
4. `pnpm build` under 500 KB.
</verification>

<success_criteria>
- src/extension.ts's createDriver splits cleanly into leader and follower paths per D-05 and D-10
- leadership.acquire() resolves BEFORE mgr.start() / activityBuilder.start() run (D-10)
- Follower path has zero Discord IPC activity (D-05 — no ConnectionManager instantiation)
- Lazy takeover path (onTakeover → bootstrapAsLeader) is wired and idempotent
- deactivate() always releases leadership (safe no-op for followers)
- All existing guardrails remain green (check-no-network, check-api-surface, check-bundle-size, pnpm test)
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-multi-window-leadership-election/05.2-02-SUMMARY.md` documenting:
- Exact structure of the leader/follower split in createDriver
- Where bootstrapAsLeader lives and how it's gated
- Guard variables added (bootstrapped, hoisted disposables)
- Decisions implemented (cite D-05, D-06, D-10, D-13)
- Bundle size delta
- Test suite delta
</output>
