---
phase: 05.1-polish-marketplace-prep
plan: "07"
subsystem: docs-polish
tags: [informational-findings, readme, code-of-conduct, companion-plugin, code-comment]
dependency_graph:
  requires: [05.1-03, 05.1-04]
  provides: [IN-01-fixed, IN-03-fixed, IN-04-fixed, IN-05-fixed, IN-06-fixed, IN-07-fixed]
  affects: [CODE_OF_CONDUCT.md, README.md, companion/claude-code-plugin/.claude-plugin/plugin.json, src/detectors/companion.ts]
tech_stack:
  added: []
  patterns: [conventional-commits-per-finding, grep-anchor-over-line-numbers]
key_files:
  created: []
  modified:
    - CODE_OF_CONDUCT.md
    - README.md
    - companion/claude-code-plugin/.claude-plugin/plugin.json
    - src/detectors/companion.ts
decisions:
  - "IN-02: leave CI action refs at @v4 — SHA pinning deferred to Phase 6 hardening per CONTEXT.md Claude's Discretion"
  - "IN-06: gate Marketplace + OpenVSX ext install blocks behind Phase 6 availability disclosure using <details> elements"
metrics:
  duration_minutes: 15
  completed: "2026-04-16"
  tasks_completed: 3
  files_modified: 4
---

# Phase 05.1 Plan 07: IN-* Informational Findings Summary

**One-liner:** Six cosmetic/doc/comment fixes addressing all 7 IN-* findings from Phase 5 review — grammar, glob, typecheck form, plugin metadata, install gating, and mtimeMs invariant comment.

## What Was Done

Addressed all 7 IN-* informational findings from `.planning/phases/05-companion-plugin-oss-hygiene-assets-readme/05-REVIEW.md`. One finding (IN-02) is an explicit no-op decision per CONTEXT.md. Six findings each received an individual atomic commit.

### IN-01 — CODE_OF_CONDUCT.md grammar fix

Dropped the duplicated preposition `at` from `enforcement at via [GitHub Issues]` → `enforcement via [GitHub Issues]`.

**Commit:** `67e300a` — `fix(05.1-07): IN-01 — drop duplicated "at" before "via" in CODE_OF_CONDUCT enforcement section`

### IN-02 — CI action SHA pinning: decided no-op

Per CONTEXT.md `<decisions>` Claude's Discretion bullet 4:

> "Whether to pin GitHub Actions to commit SHAs vs `@vN` tags as part of IN-02 (preference: leave at `@vN` — SHA pinning is a Phase 6 hardening concern, not v0.1.0)."

`.github/workflows/ci.yml` is left byte-identical to its pre-task state (`actions/checkout@v4`, `pnpm/action-setup@v4`, `actions/setup-node@v4`). Dependabot is already configured for `github-actions` weekly in `.github/dependabot.yml`, so any malicious tag-move would be flagged in a PR. SHA pinning is queued for Phase 6 hardening — not a v0.1.0 requirement.

**No commit.** Decision recorded here per plan instructions.

### IN-03 — README VSIX install glob

Changed `code --install-extension agent-mode-discord-0.1.0.vsix` to `code --install-extension agent-mode-discord-*.vsix` so the command works for any version the user downloads from GitHub Releases.

**Commit:** `195a8b3` — `docs(05.1-07): IN-03 — use glob in README VSIX install command so it works for any version`

### IN-04 — README typecheck form alignment

Changed `pnpm run typecheck` → `pnpm typecheck` in the Contributing section of README.md. CONTRIBUTING.md already used the short form and was not touched — README is the side that converges.

**Commit:** `bb30e2a` — `docs(05.1-07): IN-04 — align README typecheck form with CONTRIBUTING (pnpm typecheck, not pnpm run typecheck)`

### IN-05 — companion plugin.json metadata

Added `homepage` and `repository` (with `type`, `url`, `directory`) fields to `companion/claude-code-plugin/.claude-plugin/plugin.json`. JSON validity confirmed with `node -e "JSON.parse(...)"` before commit.

**Commit:** `d0e9076` — `docs(05.1-07): IN-05 — add homepage + repository fields to companion plugin.json`

### IN-06 — Gate Marketplace + OpenVSX install blocks

Replaced the flat `## Install` section (which showed `ext install agent-mode-dev.agent-mode-discord` as if published) with:

1. A blockquote availability notice: "Marketplace and OpenVSX listings are published as part of v0.1.0 (Phase 6). Until then, install via the Manual VSIX path below or build from source."
2. Two `<details>` disclosure elements wrapping the VS Code Marketplace and OpenVSX `ext install` commands, each labelled `(v0.1.0+)`.
3. `### Manual VSIX` and `### Companion Plugin` sections unchanged — Manual VSIX is now the primary working path until Phase 6.

**Commit:** `95b3de5` — `docs(05.1-07): IN-06 — gate Marketplace + OpenVSX install blocks behind Phase 6 availability disclosure`

### IN-07 — companion.ts mtimeMs invariant comment

Inserted a 4-line block comment immediately above `const fileExists = curr.mtimeMs > 0;` in `src/detectors/companion.ts` documenting the `fs.watchFile` all-zero Stats invariant and linking to the Node.js docs. Comment-only change; no behavior difference.

**Commit:** `a5a28e4` — `docs(05.1-07): IN-07 — pin fs.watchFile mtimeMs===0 invariant with code comment in companion detector`

## Deviations from Plan

None — plan executed exactly as written. All 7 IN-* findings addressed; 6 commits landed (IN-02 explicitly no-op per plan).

## Decisions Made

1. **IN-02 deferred to Phase 6** — SHA pinning of GitHub Actions is a hardening concern, not a v0.1.0 requirement. Dependabot already watches `github-actions` weekly. See CONTEXT.md `<decisions>` Claude's Discretion bullet 4.
2. **IN-06 uses `<details>` wrapping** — The availability blockquote appears before the collapsed blocks so readers see the Phase 6 framing before any `ext install` command. This satisfies the threat model mitigate disposition for T-05.1.07-03.

## Verification Results

- `pnpm test`: 319/319 tests pass (20 test files)
- `pnpm typecheck`: clean (no errors)
- `pnpm build`: succeeds, dist/extension.cjs 224.5 KB (43.8% of 500 KB budget)
- `pnpm check:bundle-size`: PASS
- `pnpm check:no-network`: PASS
- `pnpm check:config-keys`: PASS (14/20 keys)
- `pnpm check:api-surface`: PASS
- `pnpm check:pack-inlined`: PASS

## Known Stubs

None.

## Threat Flags

None — all changes are documentation, metadata, or a code comment. No new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check: PASSED

- `CODE_OF_CONDUCT.md`: "enforcement via [GitHub Issues]" present, "enforcement at via" absent
- `.github/workflows/ci.yml`: unchanged (`actions/checkout@v4` still present)
- `README.md`: `agent-mode-discord-*.vsix` present, `0.1.0.vsix` absent; `pnpm typecheck` present, `pnpm run typecheck` absent; `Availability:` blockquote present; 2 `<details>` blocks present
- `companion/claude-code-plugin/.claude-plugin/plugin.json`: `homepage` and `repository` present; JSON valid
- `src/detectors/companion.ts`: `fs.watchFile invariant` comment present
- All 6 commits verified in git log: 67e300a, 195a8b3, bb30e2a, d0e9076, 95b3de5, a5a28e4
