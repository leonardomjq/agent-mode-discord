---
phase: 05.1-polish-marketplace-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - assets/icon.png
autonomous: false
requirements: []
must_haves:
  truths:
    - "package.json `displayName` reads `Agent Mode — Discord Presence for Claude Code & AI Agents` (D-03)"
    - "package.json `categories` is the array `[\"Visualization\", \"Other\"]` (D-04)"
    - "package.json `keywords` is the array specified in D-05 (9 entries) and is present (currently absent)"
    - "package.json `version` reads `0.1.0` (bumped from `0.0.1` per D-06)"
    - "package.json `icon` reads `assets/icon.png` (D-07)"
    - "A 128×128 PNG file exists at `assets/icon.png` (icon asset placed by the user; otherwise the manifest field points at a non-existent file and `vsce package` will warn)"
    - "`pnpm check:config-keys` still passes (manifest changes do not alter the configuration.properties shape)"
  artifacts:
    - path: "package.json"
      provides: "Marketplace manifest with displayName / categories / keywords / version / icon polished for v0.1.0 (D-03..D-07)"
      contains: "\"version\": \"0.1.0\""
    - path: "assets/icon.png"
      provides: "128×128 PNG marketplace listing icon (D-07)"
      contains: "PNG"
  key_links:
    - from: "package.json"
      to: "assets/icon.png"
      via: "manifest `icon` field — vsce reads at package time"
      pattern: "\"icon\":\\s*\"assets/icon\\.png\""
---

<objective>
Manifest metadata pass for Marketplace + OpenVSX presentation. Five package.json field edits (D-03..D-07) plus a 128×128 icon asset (D-07).

Purpose: Marketplace search weights `displayName` heavily, the `categories` enum drives the sidebar filter, `keywords` are searchable terms, `version` must match the v0.1.0 narrative, and listings without an `icon` get a generic placeholder that kills click-through. The current manifest has the placeholder displayName "Agent Mode", a single-element `categories: ["Other"]`, no `keywords` array at all, version `0.0.1`, and no `icon` field. CONTEXT.md `<decisions>` D-03..D-07 specify exact replacements.

Output:
- `package.json` with five field edits (one new field added: `keywords`; one new field added: `icon`; three existing fields modified: `displayName`, `categories`, `version`)
- `assets/icon.png` placed by the user (HUMAN gate — see Task 1)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05.1-polish-marketplace-prep/05.1-CONTEXT.md
@package.json
@scripts/check-config-keys.mjs

<interfaces>
<!-- Current package.json (lines 1-16) state — what we are starting from: -->
<!--   "name": "agent-mode-discord", -->
<!--   "displayName": "Agent Mode",                                  ← D-03 rewrites -->
<!--   "description": "Discord Rich Presence for Claude Code and AI coding agents.", -->
<!--   "version": "0.0.1",                                           ← D-06 bumps to 0.1.0 -->
<!--   "publisher": "agent-mode-dev",                                ← LEAVE AS IS (Phase 6 / 06-06 changes this) -->
<!--   ... -->
<!--   "categories": ["Other"],                                      ← D-04 expands -->
<!--   ... -->
<!-- No "icon" field exists.   ← D-07 adds it -->
<!-- No "keywords" field exists. ← D-05 adds it -->

<!-- check-config-keys.mjs (scripts/check-config-keys.mjs) ONLY inspects pkg.contributes.configuration.properties — it does not assert on displayName / categories / keywords / version / icon. So D-03..D-07 changes are safe wrt that script. Verify by inspection before editing. -->

<!-- VS Code Extension Manifest categories enum (per Marketplace publishing docs referenced in CONTEXT.md): -->
<!--   AI, Azure, Chat, Data Science, Debuggers, Education, Extension Packs, -->
<!--   Formatters, Keymaps, Language Packs, Linters, Machine Learning, Notebooks, -->
<!--   Programming Languages, SCM Providers, Snippets, Testing, Themes, Visualization, Other -->
<!-- D-04's "Visualization" + "Other" are both valid. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: HUMAN — drop a 128×128 goblin icon at assets/icon.png</name>
  <files>assets/icon.png</files>
  <action>This is a human-action checkpoint. The user produces a 128×128 PNG and saves it to `assets/icon.png` per `<how-to-verify>` below. No automated action — Claude cannot generate brand art unsupervised.</action>
  <verify>Human confirms via `approved` or `defer` resume signal. On `approved`, automated post-check runs: `test -f assets/icon.png && file assets/icon.png | grep -q PNG`. On `defer`, no file required; Task 3 still proceeds.</verify>
  <done>User has typed `approved` (icon file in place) or `defer` (asset deferred to Phase 6 HUMAN-UAT). Either way, Task 3 runs and writes the manifest field.</done>
  <what-built>Nothing yet — this checkpoint exists because no `assets/icon.png` is present (verified: `ls assets/` shows only `CAPTURE-INSTRUCTIONS.md`). D-07 adds an `icon: "assets/icon.png"` manifest field; without the file, `vsce package` will emit a warning and the Marketplace listing will fall back to a generic placeholder.</what-built>
  <how-to-verify>
    1. Produce a 128×128 (or square ≥128×128) PNG of the goblin / project mark. Reuse existing brand art if any; otherwise generate with whatever tool you prefer (Figma, Excalidraw + export, AI image gen, etc.).
    2. Save it to `assets/icon.png` from the repo root.
    3. Verify locally:
       ```bash
       file assets/icon.png        # should report PNG image data
       stat -f%z assets/icon.png   # should be a reasonable size (< 100 KB recommended; Marketplace caps at 1 MB)
       ```
    4. (Optional) Open it in Preview / `xdg-open` / equivalent to eyeball.
    5. Type `approved` when the file is in place; type `defer` to skip — Task 3 will then add the manifest field but the asset will need to land before Phase 6 publish.
  </how-to-verify>
  <resume-signal>Type `approved` once `assets/icon.png` exists, or `defer` to leave the file for Phase 6 (manifest field still gets added in Task 3).</resume-signal>
  <acceptance_criteria>
    - On `approved`: `test -f assets/icon.png && file assets/icon.png | grep -q PNG` succeeds
    - On `defer`: noted in plan summary; Task 3 still runs and writes the `icon` field; HUMAN-UAT entry recorded for Phase 6
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify check-config-keys does not constrain displayName/categories/keywords/version/icon</name>
  <files>scripts/check-config-keys.mjs</files>
  <read_first>
    - scripts/check-config-keys.mjs (the script that asserts manifest shape — confirm it only inspects `contributes.configuration.properties`, not the top-level fields D-03..D-07 modify)
    - CONTEXT.md `<code_context>` "D-03..D-07 manifest changes need a corresponding update to `scripts/check-config-keys.mjs` if it asserts specific manifest fields (verify before editing package.json)"
  </read_first>
  <action>
    Read `scripts/check-config-keys.mjs` end-to-end. Confirm by inspection that it accesses only:
    ```js
    pkg?.contributes?.configuration?.properties
    ```
    and iterates the resulting `keys`. It does NOT read `pkg.displayName`, `pkg.categories`, `pkg.keywords`, `pkg.version`, or `pkg.icon`.

    If your inspection confirms this, no script edit is needed — proceed to Task 3 unchanged.

    If you discover the script DOES touch any of D-03..D-07's fields (which would contradict CONTEXT.md), STOP and emit a BLOCKER note instead of proceeding — the planner needs to revise.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('scripts/check-config-keys.mjs','utf8'); for (const f of ['displayName','categories','keywords','\"icon\"','\"version\"']) { if (s.includes(f) &amp;&amp; !s.includes('// ' + f)) { console.error('UNEXPECTED reference to ' + f + ' in check-config-keys.mjs'); process.exit(1); } } console.log('config-keys script touches none of D-03..D-07 fields — safe to edit package.json');"</automated>
  </verify>
  <done>
    - Inspection confirms `check-config-keys.mjs` only reads `contributes.configuration.properties`
    - Verify command exits 0 with the "safe to edit" message
  </done>
  <acceptance_criteria>
    - Verify command output contains `safe to edit package.json`
    - No edits made to `scripts/check-config-keys.mjs`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Apply five package.json manifest field changes (D-03 .. D-07)</name>
  <files>package.json</files>
  <read_first>
    - package.json (the file being modified — current `displayName: "Agent Mode"` line 3, `version: "0.0.1"` line 5, `categories: ["Other"]` line 10; no `keywords` or `icon` fields)
    - CONTEXT.md `<decisions>` D-03 through D-07 (the exact values to write)
  </read_first>
  <action>
    Edit `package.json` with these exact changes. Preserve existing JSON formatting (2-space indent, double quotes, trailing-newline convention). Apply ALL five edits in a single commit.

    **D-03 — `displayName` rewrite (line 3):**
    Change:
    ```json
    "displayName": "Agent Mode",
    ```
    To:
    ```json
    "displayName": "Agent Mode — Discord Presence for Claude Code & AI Agents",
    ```
    (Em-dash U+2014, not a hyphen. Ampersand stays literal — `package.json` is JSON, not HTML, no escape needed.)

    **D-04 — `categories` expansion (line 10):**
    Change:
    ```json
    "categories": ["Other"],
    ```
    To:
    ```json
    "categories": ["Visualization", "Other"],
    ```

    **D-05 — `keywords` array (NEW field):**
    Add immediately after the `categories` line (before `capabilities`):
    ```json
    "keywords": ["discord", "rich presence", "presence", "claude", "claude code", "cursor", "ai agent", "rpc", "status"],
    ```

    **D-06 — `version` bump (line 5):**
    Change:
    ```json
    "version": "0.0.1",
    ```
    To:
    ```json
    "version": "0.1.0",
    ```

    **D-07 — `icon` field (NEW field):**
    Add immediately after the `keywords` line (still before `capabilities`):
    ```json
    "icon": "assets/icon.png",
    ```

    Final ordered top of `package.json` should look like:
    ```json
    {
      "name": "agent-mode-discord",
      "displayName": "Agent Mode — Discord Presence for Claude Code & AI Agents",
      "description": "Discord Rich Presence for Claude Code and AI coding agents.",
      "version": "0.1.0",
      "publisher": "agent-mode-dev",
      "engines": { "vscode": "^1.93.0" },
      "main": "./dist/extension.cjs",
      "activationEvents": ["onStartupFinished"],
      "categories": ["Visualization", "Other"],
      "keywords": ["discord", "rich presence", "presence", "claude", "claude code", "cursor", "ai agent", "rpc", "status"],
      "icon": "assets/icon.png",
      "capabilities": {
        ...
      },
      ...
    }
    ```

    Do NOT touch `publisher` (`agent-mode-dev` is the placeholder Phase 6 plan 06-06 changes).
    Do NOT touch `description`, `engines`, `main`, `activationEvents`, `capabilities`, `repository`, `license`, `contributes`, `scripts`, `dependencies`, `devDependencies`, `packageManager`.
    Do NOT bump any dependency versions.

    After editing, validate JSON parses cleanly:
    ```bash
    node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"
    ```
  </action>
  <verify>
    <automated>node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); const fail=(m)=>{console.error(m);process.exit(1)}; if(p.displayName!=='Agent Mode — Discord Presence for Claude Code & AI Agents')fail('displayName mismatch'); if(JSON.stringify(p.categories)!==JSON.stringify(['Visualization','Other']))fail('categories mismatch'); if(JSON.stringify(p.keywords)!==JSON.stringify(['discord','rich presence','presence','claude','claude code','cursor','ai agent','rpc','status']))fail('keywords mismatch'); if(p.version!=='0.1.0')fail('version mismatch'); if(p.icon!=='assets/icon.png')fail('icon mismatch'); if(p.publisher!=='agent-mode-dev')fail('publisher accidentally changed'); console.log('all five D-03..D-07 fields verified');" &amp;&amp; pnpm check:config-keys &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>
    - `package.json` parses as valid JSON
    - `displayName`, `categories`, `keywords`, `version`, `icon` all match D-03..D-07 exact values
    - `publisher` still reads `agent-mode-dev` (Phase 6 boundary respected)
    - `pnpm check:config-keys` still PASSES (it only inspects `contributes.configuration.properties` per Task 2 verification)
    - `pnpm typecheck` still passes
    - Other manifest fields (description, engines, main, activationEvents, capabilities, repository, license, contributes.*, scripts, dependencies, devDependencies, packageManager) are byte-identical to the pre-edit state
  </done>
  <acceptance_criteria>
    - All five JSON.parse-based assertions in the verify command pass
    - `pnpm check:config-keys` exits 0 with the existing "14/20 keys" PASS message (key count unchanged because none of D-03..D-07 touch contributes.configuration.properties)
    - `pnpm typecheck` exits 0
    - `git diff package.json` shows ONLY the five field changes (no unrelated reformatting)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest → Marketplace search/listing | The five fields render verbatim in Marketplace search results and the listing page; consumers' first impression is built from them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1.02-01 | I (info disclosure) | None — manifest fields are public listing metadata by design | accept | Wording-only changes; no PII or secrets introduced |
| T-05.1.02-02 | E (elevation of privilege) | None — `categories` and `keywords` are display strings, not capability grants | accept | Marketplace categories enum is enforced server-side at publish time; if "Visualization"/"Other" are misspelled, vsce will reject at package time |

No new threat surface — manifest metadata changes only. The icon PNG is a static asset rendered by Marketplace; no executable surface introduced.
</threat_model>

<verification>
- All five D-03..D-07 fields verified by node-based JSON.parse assertion (see Task 3 verify command)
- `pnpm check:config-keys` exits 0 (configuration.properties unchanged)
- `pnpm typecheck` exits 0
- `assets/icon.png` exists if user typed `approved`; deferred to Phase 6 HUMAN-UAT if user typed `defer`
</verification>

<success_criteria>
- `node -p "require('./package.json').displayName"` outputs the D-03 string
- `node -p "JSON.stringify(require('./package.json').categories)"` outputs `["Visualization","Other"]`
- `node -p "JSON.stringify(require('./package.json').keywords)"` outputs the D-05 9-element array
- `node -p "require('./package.json').version"` outputs `0.1.0`
- `node -p "require('./package.json').icon"` outputs `assets/icon.png`
- `node -p "require('./package.json').publisher"` still outputs `agent-mode-dev`
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-polish-marketplace-prep/05.1-02-SUMMARY.md`
</output>
