# Phase 05.1: Polish & marketplace prep — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `05.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-16
**Phase:** 05.1-polish-marketplace-prep
**Mode:** `--auto` — Claude auto-selected the recommended option for every gray area, in a single discuss pass per workflow rule
**Areas discussed:** Bundling cleanup, Manifest metadata, README polish, CHANGELOG format, SECURITY.md wording, Multi-window VS Code, Cursor compat, IN-* findings batch

---

## Bundling cleanup

| Option | Description | Selected |
|--------|-------------|----------|
| Remove `.vscodeignore` carve-out only | esbuild already bundles @xhayper; remove `!node_modules/@xhayper/**` re-include — VSIX shrinks instantly | ✓ |
| Keep carve-out + add bundled-vs-shipped audit | Belt-and-braces: ship both, document the duplication | |
| Remove carve-out + emit THIRD_PARTY_LICENSES.md | Same as option 1 plus a build-time license aggregation step | ✓ (combined as D-01 + D-02) |

**Auto-selected:** D-01 (remove carve-out) + D-02 (emit license file). esbuild already does the bundling; the only real work is cleanup + license attribution.
**Notes:** Discovered during context gathering that `esbuild.mjs` has `bundle: true, external: ["vscode"]` — meaning my earlier marketplace-specialist analysis was wrong about needing to "add bundling". The actual gap is just dead-weight cleanup.

---

## Manifest metadata

| Option | Description | Selected |
|--------|-------------|----------|
| Bare minimum: just version bump | Skip displayName/categories/keywords/icon — those are Phase 6 polish | |
| Full metadata pass (recommended) | All 5 fields: displayName rewrite, categories, keywords, version, icon | ✓ |
| Full pass + listing description draft | Above plus a draft marketplace listing description | |

**Auto-selected:** Option 2 (D-03 through D-07). The bare-minimum option leaves the same Marketplace-search invisibility problem; the listing description draft is genuinely Phase 6 because it depends on actual screenshots and badge URLs.
**Notes:** D-07 icon depends on a 128×128 PNG existing at `assets/icon.png`. If absent, the plan creates a `[HUMAN]` task for the user to drop one in (sourced from the goblin pack art).

---

## README polish

| Option | Description | Selected |
|--------|-------------|----------|
| Just fix wording inconsistencies | Address only the IN-* findings that touch README | |
| Wording fixes + Client ID bus-factor + observability sections (recommended) | The full polish pass: bus-factor paragraph, Discord Dev Portal observability section, Privacy FAQ wording fix | ✓ |
| Above + competitive table refresh | Re-audit the competitive table against current versions of vscord/discord-vscode/RikoAppDev | |

**Auto-selected:** Option 2 (D-08, D-09, D-10). The bus-factor paragraph addresses the single most legitimate concern a careful reader will have on first install ("am I depending on this guy's Discord account forever?"). The observability section signals the project is being measured without requiring telemetry. Competitive table refresh is Phase 6 timing — those packages may move between now and the v0.1.0 listing.

---

## CHANGELOG format

| Option | Description | Selected |
|--------|-------------|----------|
| Free-form prose | One paragraph summarizing v0.1.0 | |
| Keep a Changelog 1.1.0 (recommended) | Standard format: Added / Changed / Removed / Fixed / Security per version | ✓ |
| Conventional-Commits-derived auto-changelog | Parse commit history into a generated CHANGELOG | |

**Auto-selected:** Option 2 (D-11). Marketplace's "What's New" surface reads CHANGELOG.md; the Keep a Changelog format is what readers expect. Auto-generated changelogs from commits create noise on first release; better to author by hand and switch to auto-gen for v0.2.0+ if the cadence picks up.

---

## SECURITY.md wording

| Option | Description | Selected |
|--------|-------------|----------|
| Leave as-is | "Zero outbound HTTP" is technically true; pedants will live | |
| Targeted clarification (recommended) | Distinguish extension code vs Discord IPC vs what Discord does after the IPC handoff | ✓ |
| Full threat model rewrite | Re-do the whole STRIDE table | |

**Auto-selected:** Option 2 (D-12). Current wording is accurate but invites a "you're misleading users" GitHub issue. Targeted clarification is ~3 sentences. Full rewrite is way out of scope for 5.1.

---

## Multi-window VS Code behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Skip | Pid-aware setActivity is in place; trust the design | |
| Manual smoke test + documented behavior (recommended) | Test 1/2/3+ window combinations, write `docs/MULTI-WINDOW.md` | ✓ |
| Automated multi-window harness | Build a CI job that spins multiple Discord IPC clients | |

**Auto-selected:** Option 2 (D-13). Automated harness is overkill for v0.1.0 and would need real Discord client running in CI (impractical). Manual test + doc gives Marcus/Steph a place to look when they file the inevitable "I have 5 windows open and Discord shows the wrong thing" issue.

---

## Cursor compat verification

| Option | Description | Selected |
|--------|-------------|----------|
| Skip | README troubleshooting already covers Cursor-on-Windows | |
| Documented compat statement (recommended) | `docs/CURSOR-COMPAT.md` with Cursor's VS Code baseline + known caveats | ✓ |
| Live install + smoke test | Actually install on Cursor and run the verification flow | |

**Auto-selected:** Option 2 (D-14). Live install requires the maintainer's local environment, blocks plan execution on a manual step. Documented statement is enough for v0.1.0; the actual live test is captured as backlog `999.1` for when it can be done deliberately.

---

## IN-* findings batch

| Option | Description | Selected |
|--------|-------------|----------|
| Defer to v0.2.0 | All 7 are cosmetic, none are blockers | |
| Single batched plan (recommended) | Address all 7 in one plan, one commit per finding | ✓ |
| Individual plans per finding | 7 separate plans, full GSD overhead each | |

**Auto-selected:** Option 2 (D-15). They're small enough to batch, large enough that deferring leaves obvious paper cuts on a polished release.

---

## Claude's Discretion

Captured in `05.1-CONTEXT.md` `<decisions>` section "Claude's Discretion" subsection:
- Wording of the Client ID bus-factor paragraph
- Whether THIRD_PARTY_LICENSES.md emission is a separate npm script or inline in `esbuild.mjs`
- Whether to add SPDX headers (preference: skip)
- Whether to SHA-pin GitHub Actions as part of IN-02 (preference: leave at @vN, defer to Phase 6)

## Deferred Ideas

No new deferrals from this discussion. Pre-existing deferrals carried forward:
- `@vscode/extension-telemetry` (SEED-001 → v0.2.0)
- Cursor-on-Windows live test (backlog 999.1)
- Demo GIF recording (Phase 5 HUMAN-UAT)
- GitHub branch protection enable (Phase 5 HUMAN-UAT)
- Marketplace + OpenVSX install-count badges (Phase 6, plan 06-07)
- Microsoft Partner Center publisher account (Phase 6, plan 06-06)
- Discord asset uploads (Phase 6, plan 06-01)
