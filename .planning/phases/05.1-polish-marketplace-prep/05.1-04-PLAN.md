---
phase: 05.1-polish-marketplace-prep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "CHANGELOG.md exists at the repo root, follows Keep a Changelog 1.1.0 format, and contains a v0.1.0 entry summarizing Phases 1-5 + Phase 5.1 in user-facing language (D-11)"
    - "v0.1.0 entry has Added / Changed / Removed / Fixed / Security sections (Keep a Changelog spec)"
    - "Top of file links to https://keepachangelog.com/en/1.1.0/ and notes Semantic Versioning"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Keep a Changelog 1.1.0 file documenting v0.1.0 release contents (D-11)"
      contains: "## [0.1.0]"
      min_lines: 30
  key_links:
    - from: "CHANGELOG.md"
      to: "VS Code Marketplace 'What's New' surface"
      via: "Marketplace listing renders the latest CHANGELOG entry when present"
      pattern: "## \\[0\\.1\\.0\\]"
---

<objective>
Create `CHANGELOG.md` at repo root in Keep a Changelog 1.1.0 format, with a `[0.1.0]` entry summarizing the v0.1.0 release contents in user-facing language.

Purpose: D-11. Marketplace's "What's New" tab on the listing reads from the latest CHANGELOG entry. Without it, the tab shows nothing. Keep a Changelog 1.1.0 is the de facto standard the Marketplace tooling expects.

Output: `CHANGELOG.md` (new file at repo root) with header pointing at the spec, an `[Unreleased]` placeholder, and a `[0.1.0]` entry dated 2026-04-16 with Added / Changed / Removed / Fixed / Security sections summarizing Phases 1 through 5.1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05.1-polish-marketplace-prep/05.1-CONTEXT.md
@package.json

<interfaces>
Keep a Changelog 1.1.0 reference: https://keepachangelog.com/en/1.1.0/

Required top sections:
- `# Changelog` header
- Intro paragraph linking to Keep a Changelog 1.1.0 and Semantic Versioning
- `## [Unreleased]` placeholder
- `## [X.Y.Z] - YYYY-MM-DD` per release

Per-release section order: Added, Changed, Deprecated, Removed, Fixed, Security.
D-11 specifies Added / Changed / Removed / Fixed / Security (omits Deprecated).

Source of truth for what the v0.1.0 entry summarizes:
- .planning/ROADMAP.md phases 1-5 success criteria
- 14 configuration keys in `package.json` contributes.configuration.properties
- 05.1-CONTEXT.md D-01..D-15 decisions (Phase 5.1 content rolled into v0.1.0)
- 05-REVIEW-FIX.md (WR-01..WR-06 fixes already landed from Phase 5)

File does NOT currently exist (verified by `ls CHANGELOG.md` returning ENOENT before this plan).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CHANGELOG.md with Keep a Changelog 1.1.0 structure and v0.1.0 entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/ROADMAP.md (phase success criteria — the authoritative list of what shipped in each phase)
    - .planning/phases/05.1-polish-marketplace-prep/05.1-CONTEXT.md (D-01..D-15 — Phase 5.1 additions rolled into v0.1.0)
    - .planning/phases/05-companion-plugin-oss-hygiene-assets-readme/05-REVIEW-FIX.md (WR-01..WR-06 already-landed fixes that go under "Fixed")
    - package.json (confirm 14 `contributes.configuration.properties` keys for the "Added" section's configuration line)
  </read_first>
  <action>
Create a new file `CHANGELOG.md` at the repository root with EXACTLY the content below (between the BEGIN/END markers — do not include the markers themselves). Use Markdown. End with a single trailing newline.

BEGIN CHANGELOG CONTENT
```
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog 1.1.0](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

_Nothing yet._

## [0.1.0] - 2026-04-16

Initial release. Detects Claude Code (and other AI coding agents) running in
your VS Code or Cursor integrated terminal and flips your Discord status to
AGENT_ACTIVE with goblin-voice copy — instead of "Idling" during 2-4 hour AI
sessions when you're reading diffs and prompting, not typing.

### Added

- Tiered agent detection pipeline — companion lockfile (tier-1, <100 ms),
  Shell Integration API (tier-2, <500 ms), session-file fs-watch (tier-3,
  ~1 s), terminal name polling (tier-4, ~2 s); highest active tier wins
  automatically.
- Built-in agent recognizers for `claude` (and `npx @anthropic-ai/claude-code`,
  `bunx`, `pnpm dlx` variants), `aider`, `codex` (and `npx @openai/codex`),
  `gemini`, `opencode`. Custom regex patterns extend the set via
  `agentMode.detect.customPatterns`.
- Companion Claude Code plugin (`companion/claude-code-plugin/`) — optional,
  install via `claude plugin install ./companion/claude-code-plugin` for
  tier-1 detection fidelity.
- Goblin personality pack (`presence/goblin.json`) inlined into the bundle —
  weighted message pools per state (AGENT_ACTIVE / CODING / IDLE), 2-second
  frame animations, 20-second message rotation, Fisher-Yates no-repeat,
  time-of-day bucketing.
- Custom copy pack support via `agentMode.messages.customPackPath` — 100 KB
  size cap, schema-validated, falls back to built-in pack on any error.
- 14 configuration keys under `agentMode.*` — Discord Client ID override,
  idle behavior, animations toggle, custom pack path, three privacy
  controls (filename / git branch / workspace name), four ignore lists
  (git hosts / organizations / repositories / workspaces), two detection
  tunables (custom patterns, session-file staleness threshold), debug
  verbose toggle. Settings apply on the next rotation tick — no window
  reload required.
- Privacy controls — `show` / `hide` / `hash` for workspace name, `show` /
  `hide` for filename and git branch. Hash mode uses a 6-hex-char SHA-1
  prefix of the normalized absolute path, deterministic across runs.
- Ignore lists — silence the extension entirely for matching workspaces
  (glob), repositories (regex), organizations (regex), or git hosts
  (case-insensitive substring).
- Bundled third-party license aggregate at `dist/THIRD_PARTY_LICENSES.md`,
  generated on every production build by walking the esbuild metafile.
- Companion plugin manifest declares `homepage` and `repository` so
  installers can find the project source.
- OSS hygiene — MIT `LICENSE` (2026 Leonardo Jaques), Contributor Covenant
  2.1 `CODE_OF_CONDUCT.md`, `SECURITY.md`, `CONTRIBUTING.md`, GitHub issue +
  PR templates, Dependabot config (npm + github-actions, weekly).
- 3-OS CI matrix (Ubuntu, macOS, Windows) on every PR — install / lint /
  build / bundle-size / API-surface / config-keys / no-network /
  pack-inlined / test guardrails.
- README — bus-factor + own-Client-ID override instructions, Discord
  Developer Portal observability, privacy FAQ, competitive positioning
  table, troubleshooting (Cursor on Windows / fish / cmd.exe / Flatpak
  Discord), maintainer-posture statement.
- `docs/MULTI-WINDOW.md` — documented multi-window VS Code behavior.
- `docs/CURSOR-COMPAT.md` — documented Cursor compatibility statement.

### Changed

- Marketplace metadata — `displayName` is now `Agent Mode — Discord
  Presence for Claude Code & AI Agents`; `categories` is
  `["Visualization", "Other"]`; `keywords` array added with 9 search
  terms; `icon` field points at `assets/icon.png`.
- `.vscodeignore` no longer re-includes `node_modules/@xhayper/**` —
  esbuild already bundles `@xhayper/discord-rpc` into `dist/extension.cjs`,
  so the re-include was dead weight inflating the VSIX.

### Removed

- Unreachable `companionStalenessMs` orchestrator option in
  `src/detectors/index.ts` — the option was declared but never wired
  from `extension.ts` and had no corresponding `agentMode.*` config key.
  The companion detector continues to use its internal 5-minute default.

### Fixed

- `SECURITY.md` Threat Model Notes — clarified that the extension itself
  makes zero outbound HTTP requests, while Discord IPC's onward
  transmission to Discord's servers is intentional, controlled by Discord,
  and subject to Discord's privacy policy. Earlier wording conflated the
  two.
- `SECURITY.md` companion-detector note — corrected an inaccurate claim
  that lockfiles are "size-capped (≤ 4 KB) and JSON-validated before
  parsing"; the detector reads `mtimeMs` only via `fs.watchFile` and
  never opens the file.
- `README.md` configuration-key count corrected from 20 to 14 (in both
  the prose claim and the competitive-positioning table).
- `README.md` and `assets/CAPTURE-INSTRUCTIONS.md` install commands
  reconciled — `claude plugin install ./companion/claude-code-plugin`
  from a shell, `/plugin install ./companion/claude-code-plugin` from
  a running Claude Code session.
- `assets/CAPTURE-INSTRUCTIONS.md` no longer references a non-existent
  `pnpm vscode:package` script; replaced with
  `npx @vscode/vsce package --no-dependencies`.
- `CODE_OF_CONDUCT.md` enforcement section duplicated preposition
  ("at via") fixed.
- `companion/claude-code-plugin/scripts/{start,stop}.sh` now use
  `set -euo pipefail` and a `${HOME:?...}` guard so a missing `$HOME`
  fails loudly instead of silently no-op'ing.
- `src/detectors/companion.ts` — added a code comment documenting the
  `mtimeMs > 0` ⇔ "file exists" invariant for `fs.watchFile`, so the
  pattern is not accidentally "fixed" by a future maintainer.

### Security

- Zero outbound HTTP — verified by `scripts/check-no-network.mjs` greping
  the built bundle for `http.request`, `https.request`, `fetch`,
  `undici`, `node-fetch`, and `XMLHttpRequest` patterns. Discord IPC
  (Unix socket / named pipe) is the only network surface.
- Zero VS Code proposed APIs — verified by `scripts/check-api-surface.mjs`
  greping for `(vscode as any).*` and proposed-API references.
- Configuration surface capped at ≤ 20 keys — `scripts/check-config-keys.mjs`
  blocks PRs that grow it past the budget.
- All file reads in detectors and pack loader are wrapped in `try/catch`
  with silent failure modes per project discipline; no user-visible error
  path leaks filesystem details.

[Unreleased]: https://github.com/leonardojaques/agent-mode-discord/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/leonardojaques/agent-mode-discord/releases/tag/v0.1.0
```
END CHANGELOG CONTENT

Notes:
- Use `- YYYY-MM-DD` (ASCII hyphen-space, not em-dash) on the `## [0.1.0]` line — matches Keep a Changelog 1.1.0 grammar for release headers.
- The "2-4" in "2-4 hour AI sessions" uses an ASCII hyphen to match the README's existing phrasing.
- Reference links at bottom use `github.com/leonardojaques/agent-mode-discord` (matches the `repository.url` in `package.json` line 15).
- Do NOT add a "Deprecated" section — D-11 omits it from v0.1.0 because nothing is deprecated yet.
- Do NOT add a `Why v0.1.0 and not v1.0.0?` preamble — maintainer-posture tone favors terse release notes, not essays.
  </action>
  <verify>
    <automated>node scripts/verify-changelog-05-1-04.mjs 2>/dev/null || node -e "const fs=require('fs'); const s=fs.readFileSync('CHANGELOG.md','utf8'); const checks=[['# Changelog',/^# Changelog$/m],['KAC link',/keepachangelog\.com\/en\/1\.1\.0/],['Unreleased',/^## \[Unreleased\]$/m],['0.1.0 header',/^## \[0\.1\.0\] - 2026-04-16$/m],['Added',/^### Added$/m],['Changed',/^### Changed$/m],['Removed',/^### Removed$/m],['Fixed',/^### Fixed$/m],['Security',/^### Security$/m],['compare link',/compare\/v0\.1\.0\.\.\.HEAD/],['tag link',/releases\/tag\/v0\.1\.0/]]; let ok=true; for (const [n,r] of checks){ if(!r.test(s)){ console.error('FAIL '+n); ok=false; } } if(!ok) process.exit(1); const lines=s.split('\n').length; if(lines<30){ console.error('FAIL too short: '+lines); process.exit(1); } console.log('PASS '+lines+' lines, all KAC sections present');"</automated>
  </verify>
  <done>
    - `CHANGELOG.md` exists at repo root
    - Contains `# Changelog` H1, Keep a Changelog 1.1.0 link, Semantic Versioning link
    - `## [Unreleased]` placeholder present
    - `## [0.1.0] - 2026-04-16` release header present
    - All five sections (Added / Changed / Removed / Fixed / Security) present under v0.1.0
    - Reference links for `[Unreleased]` and `[0.1.0]` present at file bottom
    - File is at least 30 lines (the verify command enforces this)
    - No trailing blank lines past the single terminating newline
  </done>
  <acceptance_criteria>
    - `grep -c '^## \[0.1.0\] - 2026-04-16$' CHANGELOG.md` returns 1
    - `grep -cE '^### (Added|Changed|Removed|Fixed|Security)$' CHANGELOG.md` returns 5
    - `grep -c 'keepachangelog.com/en/1.1.0' CHANGELOG.md` returns 1
    - `grep -c 'leonardojaques/agent-mode-discord' CHANGELOG.md` returns at least 2 (compare link + tag link)
    - File parses as valid Markdown (no unclosed code fences — `awk 'BEGIN{c=0} /^\`\`\`/{c++} END{exit c%2}' CHANGELOG.md` exits 0)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CHANGELOG → Marketplace "What's New" surface | Marketplace renders the latest CHANGELOG entry to consumers; wording shapes upgrade expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1.04-01 | R (repudiation) | CHANGELOG becomes the ground truth for what shipped in v0.1.0 | mitigate | Entry cross-references ROADMAP phase goals + 05-REVIEW-FIX.md fixes; Fixed section names the exact prior claim that was wrong (e.g., "count corrected from 20 to 14") |

No new threat surface — documentation file only. No code, no config, no runtime behavior change.
</threat_model>

<verification>
- `CHANGELOG.md` exists, matches Keep a Changelog 1.1.0 structure
- All five required sections (Added / Changed / Removed / Fixed / Security) present
- Reference links at file bottom resolve to the correct GitHub repo
- `pnpm typecheck` still passes (no code changes)
- `pnpm test` still passes (no code changes)
</verification>

<success_criteria>
- `test -f CHANGELOG.md` succeeds
- All grep assertions in the verify command pass
- File is ≥ 30 lines and renders correctly in GitHub Markdown preview
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-polish-marketplace-prep/05.1-04-SUMMARY.md`
</output>
