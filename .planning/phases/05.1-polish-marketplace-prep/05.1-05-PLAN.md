---
phase: 05.1-polish-marketplace-prep
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "SECURITY.md Threat Model Notes section accurately distinguishes (a) extension code = zero outbound HTTP, (b) Discord IPC = local socket / named pipe, (c) Discord client → Discord servers hop = intentional, controlled by Discord (D-12)"
    - "Wording no longer reads as a blanket 'no data leaves your machine' claim; pedantic readers will not conflate extension HTTP with Discord's onward forwarding"
    - "Companion-detector lockfile note (already corrected by Phase 5 WR-01 fix) is preserved unchanged"
  artifacts:
    - path: "SECURITY.md"
      provides: "Security policy with corrected Threat Model Notes section per D-12"
      contains: "Threat Model Notes"
  key_links:
    - from: "SECURITY.md (Threat Model Notes)"
      to: "Discord's privacy policy at https://discord.com/privacy"
      via: "External link in the rewritten section"
      pattern: "discord\\.com/privacy"
---

<objective>
Rewrite the "Threat Model Notes" section of `SECURITY.md` to accurately describe the three-stage path of activity data — (a) extension code → zero outbound HTTP, (b) extension → Discord IPC = local-only Unix socket / named pipe, (c) local Discord client → Discord servers = intentional, controlled by Discord, not by us.

Purpose: D-12. The current SECURITY.md says the extension makes "zero outbound HTTP requests" and that "all Discord communication uses local IPC". Both are individually true, but read together they imply that nothing ever leaves the machine — which is wrong. Discord Rich Presence's whole point is showing your status to other Discord users; the local Discord client forwards the activity payload to Discord's servers. Pedantic readers (security-minded users, employer InfoSec teams) will conflate these without the clarification. Same facts, much less misleading.

Output: `SECURITY.md` Threat Model Notes section rewritten to distinguish the three stages, with an external link to Discord's privacy policy to set the boundary of what's covered by their policy vs ours.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05.1-polish-marketplace-prep/05.1-CONTEXT.md
@SECURITY.md

<interfaces>
Current SECURITY.md "Threat Model Notes" section (verified by Read):

```
## Threat Model Notes

The extension makes **zero outbound HTTP requests**. All Discord communication
uses local IPC (Unix socket on macOS/Linux, named pipe on Windows). There is no
telemetry, no analytics, and no remote code execution path.

Lockfiles observed by the companion detector live in `~/.claude/` and are
treated as signals only — the detector reads modification time via
`fs.watchFile` and never opens or parses the file contents. A stale-mtime
threshold (default 5 minutes) guards against orphaned lockfiles from crashed
Claude Code sessions.
```

The first paragraph is what D-12 rewrites. The second paragraph (companion-detector lockfile note) was corrected by Phase 5 WR-01 fix and is now accurate — preserve it byte-for-byte.

Plan 05.1-03 (D-10) makes a parallel correction in `README.md` Privacy FAQ. Both files now describe the same three-stage path; wording should be consistent in spirit (not necessarily identical text — README is consumer-facing, SECURITY.md is policy-facing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SECURITY.md Threat Model Notes section per D-12</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md (the file being modified — current Threat Model Notes section starts at line 46; companion-detector paragraph at lines 52-56 must be preserved unchanged)
    - .planning/phases/05.1-polish-marketplace-prep/05.1-CONTEXT.md (D-12 spec)
    - .planning/phases/05-companion-plugin-oss-hygiene-assets-readme/05-REVIEW-FIX.md (WR-01 entry — confirms the companion-detector paragraph is the post-WR-01 wording and is correct as-is)
  </read_first>
  <action>
    Replace the existing Threat Model Notes section. Locate the lines starting at `## Threat Model Notes` and going through the end of the file. Replace the FIRST paragraph (currently 4 lines starting "The extension makes **zero outbound HTTP requests**.") with the three-stage breakdown. Leave the SECOND paragraph (the lockfile note, lines 52-56) byte-identical.

    The new section content (replace from `## Threat Model Notes` through the end of the lockfile paragraph):

    ```markdown
    ## Threat Model Notes

    Activity data flows through three distinct stages. Each has a different trust boundary, and conflating them creates a misleading picture of what this extension does and does not do.

    **Stage 1 — Extension code.** The extension makes **zero outbound HTTP requests**. There is no telemetry SDK, no analytics endpoint, no remote code execution path. This is verified in CI by `scripts/check-no-network.mjs`, which greps the built bundle for `http.request`, `https.request`, `fetch`, `undici`, `node-fetch`, and `XMLHttpRequest` references. A PR that introduces any of these patterns fails CI before merge.

    **Stage 2 — Extension to local Discord client (IPC).** All extension-to-Discord communication uses Discord's local IPC — a Unix domain socket on macOS/Linux (`/tmp/discord-ipc-N`), a named pipe on Windows (`\\?\pipe\discord-ipc-N`). The socket / pipe is local to the user account; it does not traverse a network interface. The activity payload (workspace name, filename, branch, status copy, computed per the user's `agentMode.privacy.*` and `agentMode.ignore.*` settings) is the only thing the extension writes to the IPC channel.

    **Stage 3 — Local Discord client to Discord's servers.** Once the local Discord client receives the activity payload, it forwards it to Discord's servers as part of normal Rich Presence operation — that's how your friends see your status. This stage is controlled entirely by the Discord desktop client, not by this extension, and is governed by [Discord's privacy policy](https://discord.com/privacy). If you do not want any activity payload reaching Discord's servers, the only options are (a) configure `agentMode.ignore.workspaces` to silence the extension entirely for sensitive workspaces, (b) close Discord, or (c) uninstall this extension.

    Lockfiles observed by the companion detector live in `~/.claude/` and are
    treated as signals only — the detector reads modification time via
    `fs.watchFile` and never opens or parses the file contents. A stale-mtime
    threshold (default 5 minutes) guards against orphaned lockfiles from crashed
    Claude Code sessions.
    ```

    The lockfile paragraph at the bottom is verbatim from the existing file (post-WR-01) — do NOT alter its wording, indentation, or line breaks.

    Do NOT touch:
    - The `# Security Policy` H1 header
    - `## Supported Versions` table
    - `## Reporting a Vulnerability` section
    - `## Scope` section
    - The companion-detector lockfile paragraph (preserved verbatim above)

    `scripts/check-no-network.mjs` invariant — D-12 only changes prose; the script asserts no network code in `src/`. No `src/` changes; the invariant is unaffected. Per CONTEXT.md `<code_context>` "D-12 SECURITY.md change needs to keep `scripts/check-no-network.mjs` invariant true — the script asserts no network code exists in `src/`; D-12 only changes prose, not code."
  </action>
  <verify>
    <automated>grep -q "^## Threat Model Notes$" SECURITY.md &amp;&amp; grep -q "Stage 1 — Extension code" SECURITY.md &amp;&amp; grep -q "Stage 2 — Extension to local Discord client" SECURITY.md &amp;&amp; grep -q "Stage 3 — Local Discord client to Discord's servers" SECURITY.md &amp;&amp; grep -q "discord.com/privacy" SECURITY.md &amp;&amp; grep -q "scripts/check-no-network.mjs" SECURITY.md &amp;&amp; grep -q "fs.watchFile\` and never opens or parses the file contents" SECURITY.md &amp;&amp; grep -q "stale-mtime" SECURITY.md &amp;&amp; pnpm check:no-network</automated>
  </verify>
  <done>
    - `SECURITY.md` has a `## Threat Model Notes` section
    - The three-stage breakdown is present (Stage 1 / 2 / 3 paragraphs)
    - Discord privacy policy link is present
    - `scripts/check-no-network.mjs` reference is present (it's the verification mechanism for the Stage 1 claim)
    - The companion-detector lockfile paragraph is preserved verbatim
    - `## Supported Versions`, `## Reporting a Vulnerability`, `## Scope` sections are unchanged (verify with `git diff SECURITY.md` showing only the Threat Model Notes section diff)
    - `pnpm check:no-network` still passes (no code changes)
    - `pnpm test` still passes (no code changes)
  </done>
  <acceptance_criteria>
    - `grep -c 'Stage [123] —' SECURITY.md` returns 3
    - `grep -c 'discord.com/privacy' SECURITY.md` returns 1
    - `grep -c 'check-no-network.mjs' SECURITY.md` returns 1
    - The lockfile paragraph still contains the exact phrase "treated as signals only — the detector reads modification time via"
    - `git diff SECURITY.md` shows the diff scope is limited to the Threat Model Notes section (no incidental edits to other sections)
    - `pnpm check:no-network` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SECURITY.md → security-minded reader | Documentation prose shapes whether reviewers (employer InfoSec, security researchers) trust the project's claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1.05-01 | R (repudiation) | Pre-D-12 wording could be read as "no data ever leaves your machine", which is false | mitigate | Three-stage breakdown explicitly names the Discord-controlled forwarding hop and links to Discord's privacy policy as the governing document for that stage |
| T-05.1.05-02 | I (info disclosure) | Documenting the IPC socket path (`/tmp/discord-ipc-N`, `\\?\pipe\discord-ipc-N`) | accept | Path is public knowledge from the @xhayper/discord-rpc README and Discord's own developer docs; documenting it is a transparency win, not a leak |

No new threat surface — documentation prose only. The companion-detector lockfile paragraph is preserved unchanged from its post-WR-01 fix state.
</threat_model>

<verification>
- D-12 three-stage breakdown present (Stage 1 / Stage 2 / Stage 3)
- Discord privacy policy link present
- Companion-detector paragraph preserved verbatim
- `pnpm check:no-network` still passes
- `pnpm test` still passes
- No `src/` files modified
</verification>

<success_criteria>
- All grep assertions in the verify command pass
- `git diff SECURITY.md` is scoped to the Threat Model Notes section
- README's Privacy FAQ rewrite (plan 05.1-03 D-10) and SECURITY.md rewrite (this plan, D-12) tell the same story without contradicting each other
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-polish-marketplace-prep/05.1-05-SUMMARY.md`
</output>
