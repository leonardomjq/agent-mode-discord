# Phase 05.1 — Open Human-UAT Items

## Multi-window VS Code smoke test (D-13)

**Updated 2026-04-16 (Phase 05.2):** Test matrix now verifies single-leader semantics, not merge-behavior. See [05.2-CONTEXT.md](../05.2-multi-window-leadership-election/05.2-CONTEXT.md) D-12 for rationale.

**Deferred at:** plan 05.1-06 Task 2 checkpoint

**What's needed:** Run the 6-scenario test matrix in [`../../../docs/MULTI-WINDOW.md`](../../../docs/MULTI-WINDOW.md) (updated in Phase 05.2 plan 03 to reflect the single-leader contract) against a live VS Code + Discord setup; record observations in the matrix's "Observed" column. The matrix now includes the leader-crash scenario (Row F) which exercises the stale-takeover path — allow ~120 s before declaring failure on that row.

**Why it's open:** Smoke test requires live Discord client + multiple VS Code windows + ~3 minutes of attention (the leader-crash row alone needs ~120 s to wait out staleness). The user opted to defer at the interactive checkpoint in Phase 05.1; the test was preserved across the Phase 05.2 behavior change because the behavior change was the whole point of the deferral.

**How to close:** Run all 6 scenarios in [`../../../docs/MULTI-WINDOW.md`](../../../docs/MULTI-WINDOW.md), edit that file to replace the `_pending_` cells with concrete observations, then remove this entry from `05.1-HUMAN-UAT.md`.
