---
phase: 05.1-polish-marketplace-prep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Privacy FAQ contains a 'Bus factor / using your own Client ID' subsection teaching users about the agentMode.clientId override (D-08)"
    - "README contains an 'Observability' section documenting Discord Developer Portal + Marketplace/OpenVSX dashboard metrics (D-09)"
    - "Privacy FAQ 'What does this extension send to the internet?' answer accurately distinguishes extension HTTP (zero) from Discord IPC (intentional, controlled by Discord) (D-10)"
  artifacts:
    - path: "README.md"
      provides: "Polished v0.1.0 README with three new/revised sections (D-08 bus factor, D-09 observability, D-10 corrected privacy wording)"
      contains: "agentMode.clientId"
  key_links:
    - from: "README.md (Privacy FAQ → Bus factor subsection)"
      to: "src/extension.ts:65 (bootCfg.clientId wiring) and src/rpc/client.ts:9 (DEFAULT_CLIENT_ID + AGENT_MODE_CLIENT_ID env override)"
      via: "Documented config key `agentMode.clientId` already wired in code"
      pattern: "agentMode\\.clientId"
    - from: "README.md (Observability section)"
      to: "Discord Developer Portal + Marketplace/OpenVSX dashboards (external surfaces)"
      via: "Prose only — no code wiring"
      pattern: "Observability|Developer Portal"
---

<objective>
Polish the README with three discrete sections: bus-factor / Client ID override (D-08), Discord Dev Portal + Marketplace observability (D-09), and a corrected Privacy FAQ entry that no longer conflates extension HTTP with Discord IPC (D-10).

Purpose: D-08 closes a real concern — if the maintainer's Discord account is ever lost, every install is silently broken because they all hit the same Client ID; the fix is teaching users that `agentMode.clientId` already exists as a 2-minute override path. D-09 sets reader expectations about what's measurable without writing any extension-side telemetry (Discord Dev Portal exposes DAU/MAU/activity counts for the bundled Client ID; Marketplace + OpenVSX expose install counts). D-10 corrects a pedantic-but-real misstatement: "zero outbound HTTP" is true of the extension's own code, but Discord IPC hands off to the local Discord client which forwards to Discord's servers — that path is intentional and controlled by Discord, not by us. Same facts, much less misleading.

Output:
- `README.md` Privacy FAQ "What does this extension send to the internet?" answer rewritten per D-10
- New "Bus factor / using your own Client ID" subsection inserted under Privacy FAQ per D-08
- New top-level "Observability" section inserted between Privacy FAQ and Competitive Positioning per D-09
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05.1-polish-marketplace-prep/05.1-CONTEXT.md
@README.md
@src/extension.ts
@src/rpc/client.ts

<interfaces>
<!-- Current README.md structure (verified by reading): -->
<!--   Line 124: "## Privacy FAQ" header -->
<!--   Lines 126-127: "**What does this extension send to the internet?** Nothing. ..." ← D-10 rewrites -->
<!--   Lines 129-136: other Q&A entries (workspace name, hide sensitive info, employer) -->
<!--   Line 138: "---" separator -->
<!--   Line 140: "## Competitive Positioning" header -->

<!-- Code-side already-shipped wiring D-08 documents (do NOT modify code, just point to it): -->
<!--   src/extension.ts:65 → const bootCfg = readConfig();  // bootCfg.clientId pulled from agentMode.clientId -->
<!--   src/extension.ts:70 → createConnectionManager(bootCfg.clientId, process.pid, realBackoffDeps); -->
<!--   src/rpc/client.ts:9-10 → DEFAULT_CLIENT_ID = process.env.AGENT_MODE_CLIENT_ID ?? "1493599126217297981" -->

<!-- CONTEXT.md <specifics> guidance for D-08 wording: -->
<!--   "should explicitly say 'if I get hit by a bus' or similar — match the maintainer-posture tone -->
<!--    established in CONTRIBUTING.md ('solo project, maintained on my own schedule, passion project not a product')" -->

<!-- CONTEXT.md <specifics> guidance for D-09 wording: -->
<!--   "should NOT promise the user that their data is being analyzed — it documents what's available, -->
<!--    not what we do with it. Wording matters: 'Discord exposes the following metrics for the bundled -->
<!--    Client ID' not 'We track these metrics'." -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Privacy FAQ "internet" Q&A for accuracy (D-10)</name>
  <files>README.md</files>
  <read_first>
    - README.md (the file being modified — current "What does this extension send to the internet?" Q&A is at lines 126-127)
    - CONTEXT.md `<decisions>` D-10 (the precise distinction to draw)
  </read_first>
  <action>
    Locate the existing Q&A block at README.md lines 126-127:

    ```markdown
    **What does this extension send to the internet?**
    Nothing. It communicates exclusively over Discord's local IPC socket (Unix socket on macOS/Linux, named pipe on Windows). Zero outbound HTTP requests. Verified by a CI check that greps the built bundle for HTTP patterns.
    ```

    Replace with:

    ```markdown
    **What does this extension send to the internet?**
    The extension itself makes **zero outbound HTTP requests** — verified by a CI check (`scripts/check-no-network.mjs`) that greps the built bundle for `http.request`, `https.request`, `fetch`, `undici`, `node-fetch`, and `XMLHttpRequest`. All communication uses Discord's local IPC (Unix socket on macOS/Linux, named pipe on Windows) to the Discord desktop client running on your machine.

    From there, the local Discord client forwards your activity payload (workspace name, filename, branch, status copy — whatever you have not redacted via the `agentMode.privacy.*` settings) to Discord's servers. That last hop is part of how Discord Rich Presence works, controlled by the Discord client itself, not by this extension. Anything Discord receives is subject to [Discord's privacy policy](https://discord.com/privacy) — not ours.
    ```

    Preserve every other Q&A entry in the Privacy FAQ section unchanged. Preserve all surrounding markdown structure (`---` separators, header levels).
  </action>
  <verify>
    <automated>grep -q "extension itself makes \*\*zero outbound HTTP requests\*\*" README.md &amp;&amp; grep -q "discord.com/privacy" README.md &amp;&amp; ! grep -q "^Nothing\. It communicates exclusively" README.md</automated>
  </verify>
  <done>
    - New wording present (regex-grep above passes)
    - Old "Nothing. It communicates exclusively..." sentence is gone
    - Discord privacy policy link present
    - Other Privacy FAQ entries (workspace name, hide sensitive info, employer) untouched
  </done>
  <acceptance_criteria>
    - `grep -c 'extension itself makes' README.md` returns at least 1
    - `grep -c 'discord.com/privacy' README.md` returns at least 1
    - `grep -c 'agentMode.privacy' README.md` returns at least 1 (cross-reference to existing config keys preserved)
    - The 4 other Q&A pairs ("What shows in Discord by default?", "How do I hide sensitive info?", "Can my employer see my activity?") are byte-identical to pre-edit
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add "Bus factor / using your own Client ID" subsection under Privacy FAQ (D-08)</name>
  <files>README.md</files>
  <read_first>
    - README.md (still the file being modified — Task 1 just rewrote the first Q&A; this task ADDS a new H3 subsection at the END of the Privacy FAQ section, before the `---` separator that opens Competitive Positioning)
    - src/rpc/client.ts (lines 9-10 — confirms `DEFAULT_CLIENT_ID = process.env.AGENT_MODE_CLIENT_ID ?? "1493599126217297981"` already exists; the README paragraph teaches users about this existing wiring, no code changes needed)
    - src/extension.ts (line 65 — confirms `bootCfg.clientId` flows from `agentMode.clientId` config; line 70 hands it to `createConnectionManager`)
    - CONTEXT.md `<decisions>` D-08 + `<specifics>` "if I get hit by a bus" guidance
  </read_first>
  <action>
    Locate the Privacy FAQ section's last Q&A ("Can my employer see my activity?") around lines 135-136. After that Q&A but BEFORE the `---` separator that opens Competitive Positioning (around line 138), insert this new H3 subsection:

    ```markdown
    ### Bus factor — using your own Client ID

    Every install of this extension talks to the same Discord Application — Client ID `1493599126217297981`, owned by the maintainer ([Leonardo Jaques](https://github.com/leonardojaques)). That works fine until it doesn't: if I lose access to the Discord developer account (lost MFA device, account banned, hit by the proverbial bus), all installs go silent until someone files a PR with a new ID.

    To insulate yourself from that, register your own Discord Application in 2 minutes and override the bundled Client ID in your VS Code settings:

    1. Open the [Discord Developer Portal](https://discord.com/developers/applications) and click **New Application**. Give it any name — only you and your Discord friends will see it.
    2. Copy the **Application ID** from the General Information page.
    3. In VS Code, open settings (`Cmd/Ctrl + ,`), search for `agentMode.clientId`, and paste your ID into the override field.
    4. Reload the window (or wait for the next rotation tick). Your presence is now flowing through your own Discord application; no further dependency on this project's bundled ID.

    The override path also accepts an environment variable for ad-hoc / CI use: `AGENT_MODE_CLIENT_ID=your-id-here code .` (see [`src/rpc/client.ts`](src/rpc/client.ts) line 9). The setting wins over the env var when both are present.

    This is also the recommended path if you want to upload custom large/small assets (e.g., your own goblin art) — Discord Rich Presence asset uploads are scoped to the Application that owns them.
    ```

    Match the maintainer-posture tone established in CONTRIBUTING.md (terse, defensive, no apology) — the wording above already does (see "if I lose access to the Discord developer account ... or hit by the proverbial bus").

    Do NOT touch any other section. Do NOT renumber existing Q&A. The new subsection is an H3 (`###`) under the existing H2 (`##`) "Privacy FAQ".
  </action>
  <verify>
    <automated>grep -q "### Bus factor — using your own Client ID" README.md &amp;&amp; grep -q "1493599126217297981" README.md &amp;&amp; grep -q "AGENT_MODE_CLIENT_ID" README.md &amp;&amp; grep -q "discord.com/developers/applications" README.md</automated>
  </verify>
  <done>
    - H3 subsection "Bus factor — using your own Client ID" exists as the last child of the Privacy FAQ H2
    - The bundled client ID literal (`1493599126217297981`) appears so users can sanity-check what they're overriding
    - Both override paths are documented (settings UI key `agentMode.clientId` + env var `AGENT_MODE_CLIENT_ID`)
    - Cross-reference link to `src/rpc/client.ts` line 9 is present
    - Maintainer-posture tone preserved (no apology, "if I get hit by a bus" energy)
  </done>
  <acceptance_criteria>
    - `grep -c '### Bus factor' README.md` returns 1
    - `grep -c 'agentMode.clientId' README.md` returns at least 2 (the new subsection + the existing Configuration table at line 111)
    - The new subsection appears AFTER "Can my employer see my activity?" Q&A and BEFORE the `---` separator preceding "## Competitive Positioning"
    - No other section disturbed (verify by `git diff README.md` — only an addition between the marker lines)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add "Observability" section between Privacy FAQ and Competitive Positioning (D-09)</name>
  <files>README.md</files>
  <read_first>
    - README.md (Task 2 just added the bus-factor subsection at the end of Privacy FAQ; this task adds a NEW H2 section "Observability" between the closing `---` of Privacy FAQ and the opening `## Competitive Positioning` header at line 140)
    - CONTEXT.md `<decisions>` D-09 + `<specifics>` "Discord exposes the following metrics for the bundled Client ID — not 'We track these metrics'"
  </read_first>
  <action>
    Locate the `---` separator that currently sits between Privacy FAQ and Competitive Positioning (was line 138 before Task 2, now slightly later because of Task 2's insertion). After that `---`, insert this new H2 section + a new `---` after it:

    ```markdown
    ## Observability

    This project doesn't ship telemetry — no analytics SDK, no opt-in tracker, no extension-side metrics surface (a `@vscode/extension-telemetry` opt-in is captured in `SEED-001` for v0.2.0+ but is not in v0.1.0). What follows is a description of what is *already* visible through external surfaces, so you know what's measurable without writing any extension code:

    **Discord Developer Portal** exposes the following metrics for the bundled Client ID `1493599126217297981`:

    - **DAU / MAU** — daily and monthly active users with this extension talking to Discord
    - **Activity counts** — how often Rich Presence payloads are sent
    - **Authorized installs** — total Discord accounts that have ever connected

    These are visible only to the Discord developer account that owns the Application. They aren't public, and they aren't sent anywhere else. If you've followed the *Bus factor* section above and registered your own Client ID, these metrics for your install flow into your own Developer Portal dashboard, not mine.

    **VS Code Marketplace** and **OpenVSX** expose extension install counts and version-over-version uptake on the listing page once Phase 6 publishes v0.1.0 — those numbers will be live there for anyone to look at.

    None of this requires extension-side telemetry code. The Marketplace dashboard and Discord Developer Portal are the primary observability surfaces.

    ---
    ```

    Wording per CONTEXT.md `<specifics>`: documents what's available, doesn't promise analysis ("Discord exposes the following metrics" not "we track these metrics").

    Do NOT touch the "## Competitive Positioning" header that follows. Do NOT add Marketplace/OpenVSX shields.io badges (those are Phase 6 plan 06-07 — see CONTEXT.md `<deferred>`).
  </action>
  <verify>
    <automated>grep -q "^## Observability$" README.md &amp;&amp; grep -q "Discord Developer Portal" README.md &amp;&amp; grep -q "DAU / MAU" README.md &amp;&amp; grep -q "extension code" README.md &amp;&amp; grep -q "^## Competitive Positioning$" README.md</automated>
  </verify>
  <done>
    - New H2 "## Observability" exists in the README
    - It sits between Privacy FAQ and Competitive Positioning (verify by viewing `grep -n '^## ' README.md` — Observability appears between them)
    - Mentions Discord Developer Portal as primary surface
    - Documents DAU/MAU, Activity counts, Authorized installs (the three metrics CONTEXT.md D-09 calls out)
    - Mentions Marketplace + OpenVSX install counts as a Phase 6 future-tense surface
    - Does NOT make first-person "we track" claims (verify: `grep -i 'we track\|we collect\|we monitor' README.md` returns nothing)
    - Existing Competitive Positioning H2 still follows
  </done>
  <acceptance_criteria>
    - `grep -c '^## Observability$' README.md` returns 1
    - `grep -c '^## Competitive Positioning$' README.md` returns 1
    - In `grep -n '^## ' README.md` output, "## Observability" line number is less than "## Competitive Positioning" line number
    - `grep -ic 'we track\|we collect\|we monitor' README.md` returns 0 (CONTEXT.md `<specifics>` wording rule)
    - `pnpm typecheck` still passes (no code changes; just markdown)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → reader expectations | Documentation prose shapes user trust; D-10 corrects a misstatement that could create false security expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1.03-01 | R (repudiation) | D-10 corrects a previously-incorrect "zero outbound HTTP" claim that conflated extension code with Discord IPC | mitigate | New wording explicitly distinguishes extension code (zero HTTP, CI-verified) from Discord IPC (intentional, controlled by Discord, subject to Discord's privacy policy) |
| T-05.1.03-02 | I (info disclosure) | D-08 publishes the bundled Client ID literal in the README | accept | Client ID is already public — anyone who installs the extension and inspects RPC traffic sees it; documenting it is a transparency win, not a leak |

No new threat surface — README prose changes only. The Bus factor subsection teaches users about an existing config key (`agentMode.clientId`) wired in `src/extension.ts:65` since Phase 1; no new code path.
</threat_model>

<verification>
- D-08: "Bus factor" H3 subsection exists with both override paths documented
- D-09: "Observability" H2 section exists, wording matches CONTEXT.md `<specifics>` (descriptive, not promissory)
- D-10: Privacy FAQ "internet" Q&A no longer makes the conflated claim
- `pnpm typecheck` passes (no code changes)
- `pnpm test` passes (no code changes)
</verification>

<success_criteria>
- All three D-08/D-09/D-10 deliverables verified by grep assertions
- README still renders cleanly in GitHub markdown preview (visual check; no broken markdown syntax)
- No code edits, no behavior change in shipped extension
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-polish-marketplace-prep/05.1-03-SUMMARY.md`
</output>
