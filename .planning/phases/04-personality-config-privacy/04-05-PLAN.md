---
phase: 04
plan: 05
type: execute
wave: 1
depends_on: [04-00]
files_modified:
  - src/presence/goblin.json
  - esbuild.mjs
  - scripts/check-pack-inlined.mjs
  - package.json
autonomous: true
requirements: [PERS-01]
threat_refs: []
tags: [presence, pack-data, goblin]

must_haves:
  truths:
    - "src/presence/goblin.json exists with id=goblin, version=1, pools.AGENT_ACTIVE (object with _primary + claude + codex), pools.CODING (array), pools.IDLE (array), timeOfDay (4 buckets)"
    - "Content matches CONTEXT D-05 canonical pack verbatim — no silent edits"
    - "Every message ≤ 50 chars (D-04); frame arrays preserve cooking.. / thinking.. sequences"
    - "esbuild config inlines the JSON into dist/extension.cjs so VSIX ships with goblin content embedded (addresses Pitfall 8)"
    - "Pack passes validatePack() from plan 04-01 (goblin.json is the reference valid input)"
  artifacts:
    - path: "src/presence/goblin.json"
      provides: "Canonical built-in goblin pack — the only built-in copy pack in v0.1 (PERS-01)"
      contains: "\"id\": \"goblin\""
    - path: "esbuild.mjs"
      provides: "Loader rule ensuring goblin.json is inlined (or verified already inlined via default loader)"
      contains: "goblin.json"
    - path: "scripts/check-pack-inlined.mjs"
      provides: "Named guardrail that asserts the canonical goblin strings are present in dist/extension.cjs (reviewer R1: script, not raw grep)"
      contains: "letting it cook"
  key_links:
    - from: "src/presence/packLoader.ts (future consumer)"
      to: "src/presence/goblin.json"
      via: "import goblin from \"./goblin.json\" (esbuild inline)"
      pattern: "from.*goblin"
---

<objective>
Commit the canonical goblin pack as `src/presence/goblin.json` with the EXACT content locked in CONTEXT D-05 (no editorializing), and verify/configure esbuild so the JSON is embedded in the built bundle (Pitfall 8 from 04-RESEARCH.md). This is the `builtin` Pack that `loadPack()` from plan 04-01 falls back to when no custom pack is configured.

Purpose: Plan 04-02 (animator) constructs its first rotation from this pack. Plan 04-04 (activityBuilder) passes this pack to `loadPack()`. Ship the data first so downstream wiring is a one-line import.

Output:
- `src/presence/goblin.json` — 1 file, ~50 lines
- `esbuild.mjs` — loader rule (or a verification comment if the default already inlines JSON)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-personality-config-privacy/04-CONTEXT.md
@.planning/phases/04-personality-config-privacy/04-RESEARCH.md
@esbuild.mjs
@src/presence/packLoader.ts
@src/presence/types.ts
</context>

<threat_model>
## Trust Boundaries
None introduced. Built-in pack is author-controlled data shipped in the VSIX.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering (VSIX supply chain) | goblin.json in bundle | accept | Static data checked into git; CI build reproduces it; any tampering surfaces in PR diff |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Commit src/presence/goblin.json verbatim from CONTEXT D-05</name>
  <files>src/presence/goblin.json</files>
  <read_first>
    - .planning/phases/04-personality-config-privacy/04-CONTEXT.md D-05 (canonical pack content — copy VERBATIM, do not edit copy lines)
    - .planning/phases/04-personality-config-privacy/04-CONTEXT.md D-04 (≤50 char target — verify each line; all current D-05 lines comply)
    - src/presence/types.ts (Pack type — must match)
    - src/presence/packLoader.ts (validatePack — this file must pass it)
  </read_first>
  <action>
    Create `src/presence/goblin.json` with exactly this content (copy-paste from CONTEXT D-05, trailing newline, 2-space indent):

    ```json
    {
      "id": "goblin",
      "version": 1,
      "pools": {
        "AGENT_ACTIVE": {
          "_primary": [
            ["cooking.", "cooking..", "cooking..."],
            "shipping",
            "building",
            "in the kitchen",
            "letting it cook",
            "the agent is cooking",
            "building, afk",
            "pair-coded with AI",
            "agent-augmented",
            "vibe shipping",
            "prompt → PR",
            "delegated",
            "agent on the case",
            ["thinking.", "thinking..", "thinking..."],
            "outsourced the boring parts"
          ],
          "claude": [
            "shipping with claude",
            "claude locked in",
            "letting claude cook",
            "pair-coded with claude"
          ],
          "codex": [
            "shipping with codex",
            "codex locked in"
          ]
        },
        "CODING": [
          "reviewing the diff",
          "approving the plan",
          "queueing the next prompt",
          "steering the agent",
          "between prompts",
          "the review pass",
          "checking the work"
        ],
        "IDLE": [
          "context cooling",
          "agent.sleep()",
          "touching grass",
          "afk, still building",
          "stepped away",
          "brb, thinking",
          "mid-brainstorm"
        ]
      },
      "timeOfDay": {
        "lateNight": ["3am build session", "the night shift", "shipping before sleep"],
        "morning":   ["first prompt of the day", "morning build", "caffeine-loaded"],
        "afternoon": ["post-lunch build", "3pm focus sprint"],
        "evening":   ["golden hour shipping", "evening build session"]
      }
    }
    ```

    **No editorial changes.** The arrow char `→` in "prompt → PR" is intentional (not `->`). No emojis (D-specifics).
  </action>
  <verify>
    <automated>node -e "const p=require('./src/presence/goblin.json'); const {validatePack}=require('./src/presence/packLoader.ts'); /* via ts-node? skip */ console.log(p.id, p.version, Object.keys(p.pools).length)" 2>/dev/null || node -e "const p=require('./src/presence/goblin.json'); if(p.id!=='goblin'||p.version!==1||!p.pools.AGENT_ACTIVE._primary||!Array.isArray(p.pools.CODING)||!Array.isArray(p.pools.IDLE)){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/presence/goblin.json` returns 0
    - `node -e "const p=require('./src/presence/goblin.json'); process.exit(p.id==='goblin'&&p.version===1?0:1)"` exits 0
    - `node -e "const p=require('./src/presence/goblin.json'); process.exit(p.pools.AGENT_ACTIVE._primary.length===15?0:1)"` exits 0 (15 _primary entries)
    - `node -e "const p=require('./src/presence/goblin.json'); process.exit(p.pools.AGENT_ACTIVE.claude.length===4?0:1)"` exits 0
    - `node -e "const p=require('./src/presence/goblin.json'); const all=[...p.pools.AGENT_ACTIVE._primary,...p.pools.CODING,...p.pools.IDLE].flat(); const over=all.filter(m=>m.length>50); process.exit(over.length===0?0:1)"` exits 0 (every message ≤ 50 chars per D-04)
    - `grep -c '→' src/presence/goblin.json` = 1 (the intentional arrow in "prompt → PR")
  </acceptance_criteria>
  <done>
    goblin.json exists, parses, conforms to Pack schema, passes D-04 length audit, matches CONTEXT D-05 verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify esbuild inlines goblin.json into dist/extension.cjs</name>
  <files>esbuild.mjs</files>
  <read_first>
    - esbuild.mjs (current config — check whether JSON loader is already default or needs an explicit `loader: { ".json": "json" }` entry)
    - .planning/phases/04-personality-config-privacy/04-RESEARCH.md §"Pitfall 8" (esbuild JSON semantics)
  </read_first>
  <action>
    Read esbuild.mjs. **If** the config already uses esbuild's default JSON loader (no explicit exclusion, no `loader: { ".json": "file" }`), then JSON imports will be inlined by default — no change needed; add a short comment block noting "goblin.json inlined via esbuild default JSON loader; verified via dist/extension.cjs search post-build (plan 04-04)".

    **If** the config explicitly disables JSON inlining or excludes the presence directory, add an explicit `loader: { ".json": "json" }` entry (or merge into existing loader map) so the goblin.json import inlines.

    Do NOT add a new entryPoint or external. The goblin pack must ship embedded in the single bundle file; Phase 4 bundle headroom is ~288 KB, goblin.json is ~1.5 KB — trivial budget impact.

    Verify post-build: run `pnpm build`, then `node scripts/check-pack-inlined.mjs` should exit 0 and print `PASS`. (We don't have dist/ yet at Wave-1 time; this check belongs to the plan 04-04 acceptance gate. For this task, the acceptance is config-level plus creating the named script.)

    **Create scripts/check-pack-inlined.mjs** (reviewer R1: replace raw `grep -q` in acceptance with a named script):
    ```js
    #!/usr/bin/env node
    import { readFileSync, existsSync } from "node:fs";
    const BUNDLE = "dist/extension.cjs";
    if (!existsSync(BUNDLE)) {
      console.error(`FAIL: ${BUNDLE} not found — run \`pnpm build\` first.`);
      process.exit(1);
    }
    const src = readFileSync(BUNDLE, "utf8");
    // Canonical strings that prove goblin.json was inlined by esbuild.
    // Pick two distinct strings from D-05 so a partial embed would still fail.
    const CANONICAL = ["letting it cook", "the agent is cooking"];
    const missing = CANONICAL.filter((needle) => !src.includes(needle));
    if (missing.length > 0) {
      console.error(`FAIL: goblin pack not inlined. Missing strings in ${BUNDLE}: ${missing.join(", ")}`);
      console.error("Verify esbuild JSON loader is enabled; see 04-RESEARCH.md Pitfall 8.");
      process.exit(1);
    }
    console.log(`PASS: goblin pack inlined (${CANONICAL.length}/${CANONICAL.length} canonical strings found in ${src.length} bytes).`);
    ```

    **Edit package.json scripts** — add:
    ```
    "check:pack-inlined": "node scripts/check-pack-inlined.mjs",
    ```
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; node scripts/check-pack-inlined.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0 (no warnings on JSON loader)
    - `node scripts/check-pack-inlined.mjs` exits 0 and prints "PASS: goblin pack inlined"
    - `grep -c "\"check:pack-inlined\"" package.json` = 1 (script wired)
    - Bundle size delta: `node scripts/check-bundle-size.mjs` still passes (goblin.json adds ~1.5 KB; current 212 KB → ~214 KB, well under 500 KB)
  </acceptance_criteria>
  <done>
    goblin.json is inlined in the build output; acceptance grep finds canonical goblin strings in dist/extension.cjs.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` PASS
- `node scripts/check-pack-inlined.mjs` PASS (replaces raw grep per reviewer R1)
- `pnpm check:bundle-size` PASS
- No edits to any src/ file under detectors/, state/, rpc/, or privacy.ts
</verification>

<success_criteria>
Plan complete when:
- goblin.json committed verbatim from D-05
- Every message ≤ 50 chars
- `check:pack-inlined` passes against built bundle (canonical strings present)
- Bundle-size guardrail still green
</success_criteria>

<output>
After completion, create `.planning/phases/04-personality-config-privacy/04-05-SUMMARY.md` per template.
</output>
