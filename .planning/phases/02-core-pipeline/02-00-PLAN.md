---
phase: 02
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/state.machine.test.ts
  - test/rpc.throttle.test.ts
  - test/rpc.client.backoff.test.ts
  - test/privacy.test.ts
  - test/detectors.editor.test.ts
  - test/detectors.git.test.ts
  - scripts/check-api-surface.mjs
  - .planning/phases/02-core-pipeline/02-HUMAN-UAT.md
autonomous: true
requirements: []
tags: [infra, test-scaffolding, check-api-surface, nyquist]

must_haves:
  truths:
    - "Every Phase 2 src module has a matching vitest test file present at repo before the src module is written"
    - "`pnpm check:api-surface` fails if any file under `src/state/**` or `src/rpc/throttle.ts` or `src/privacy.ts` imports `vscode` at runtime"
    - "02-HUMAN-UAT.md enumerates the three manual-only dev-host checks (SC-1 IDLE timer, SC-3 Discord kill/restart replay, SC-4 two-window pid isolation)"
  artifacts:
    - path: "test/state.machine.test.ts"
      provides: "Reducer test stub (STATE-01..05 placeholders)"
      contains: "describe(\"state machine reducer\""
    - path: "test/rpc.throttle.test.ts"
      provides: "Throttle test stub (RPC-02 / STATE-06 placeholders)"
      contains: "describe(\"rpc throttle"
    - path: "test/rpc.client.backoff.test.ts"
      provides: "Connection manager backoff test stub (RPC-03 / RPC-04 placeholders)"
      contains: "describe(\"connection manager backoff\""
    - path: "test/privacy.test.ts"
      provides: "Privacy redact test stub (show / hide / hash)"
      contains: "describe(\"privacy.redact\""
    - path: "test/detectors.editor.test.ts"
      provides: "Editor detector test stub (vi.mock vscode)"
      contains: "vi.mock(\"vscode\""
    - path: "test/detectors.git.test.ts"
      provides: "Git detector test stub (vi.mock vscode + git extension stub)"
      contains: "vi.mock(\"vscode\""
    - path: "scripts/check-api-surface.mjs"
      provides: "Extended with path-scoped vscode-import ban for pure-core (D-16)"
      contains: "PURE_CORE"
    - path: ".planning/phases/02-core-pipeline/02-HUMAN-UAT.md"
      provides: "Dev Host manual verification checklist (SC-1, SC-3, SC-4)"
      contains: "Two-window pid isolation"
  key_links:
    - from: "scripts/check-api-surface.mjs"
      to: "src/state/**, src/rpc/throttle.ts, src/privacy.ts"
      via: "path-scoped VSCODE_IMPORT regex guard"
      pattern: "PURE_CORE"
    - from: "test/*.test.ts (six new files)"
      to: "src modules landed in plans 02-01..02-07"
      via: "vitest import-by-path"
      pattern: "from \"\\.\\./src/"
---

<objective>
Wave 0 infrastructure: create all six vitest test files as empty-but-valid stubs, extend `scripts/check-api-surface.mjs` with a path-scoped `vscode`-import ban for pure-core modules (D-16), and author `02-HUMAN-UAT.md` with the three manual-only dev-host verification steps. No `src/` code lands in this plan — it locks the test-before-code contract and the pure-core boundary before any pure-core file is written.

Purpose: Satisfy the Nyquist rule (every downstream task's `<automated>` must target a test file that already exists) and lock D-16 (pure-core vscode-import ban) before any `src/state/**` / `src/rpc/throttle.ts` / `src/privacy.ts` lands. Prevents the common failure where pure-core accidentally imports vscode and the violation only surfaces at code review.

Output: 6 test stubs (skipped `it.todo` placeholders that compile + run green), extended `check-api-surface.mjs`, and `02-HUMAN-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pipeline/02-CONTEXT.md
@.planning/phases/02-core-pipeline/02-RESEARCH.md
@.planning/phases/02-core-pipeline/02-VALIDATION.md
@.planning/phases/01-skeleton-rpc-seam/01-04-SUMMARY.md
@scripts/check-api-surface.mjs
@test/rpc.client.smoke.test.ts

<interfaces>
From scripts/check-api-surface.mjs (existing):
- Walks `src/` collecting `.ts` files
- BAD_CAST `/\(\s*vscode\s+as\s+any\s*\)/`
- BAD_ANY `/\(\s*vscode\s*:\s*any\s*\)/`
- PROPOSED_API check against `package.json.enabledApiProposals`
- exits 1 on any failure, prints PASS with file count on success

Existing test harness pattern (test/rpc.client.smoke.test.ts):
- `import { describe, it, expect, vi, beforeEach, afterEach } from "vitest"`
- `vi.mock("@xhayper/discord-rpc", () => ({ Client: class { ... } }))`
- top-level `vi.mock` hoists before imports
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create six vitest test stub files with describe blocks and it.todo placeholders</name>
  <files>
    test/state.machine.test.ts,
    test/rpc.throttle.test.ts,
    test/rpc.client.backoff.test.ts,
    test/privacy.test.ts,
    test/detectors.editor.test.ts,
    test/detectors.git.test.ts
  </files>
  <read_first>
    - test/rpc.client.smoke.test.ts (existing mock-factory pattern — replicate structure)
    - .planning/phases/02-core-pipeline/02-RESEARCH.md §Code Examples §3 (throttle test recipe), §7 (extended mock factory for ready/disconnected)
    - .planning/phases/02-core-pipeline/02-VALIDATION.md (per-task verification map — test names land here)
    - .planning/phases/02-core-pipeline/02-CONTEXT.md D-19 (test file list)
  </read_first>
  <action>
Create six test files as stubs. Each file MUST:
1. Import `describe`, `it`, `expect`, `vi`, `beforeEach`, `afterEach` from `vitest`.
2. Contain a top-level `describe(...)` block with the exact name below.
3. Contain `it.todo(...)` placeholders for each planned assertion listed below — `it.todo` passes vitest without implementation, so `pnpm test` stays green.
4. NOT import any `src/*` module yet (those modules don't exist — plans 02-01..02-07 create them). Imports may be added when the plan landing the source runs.
5. Compile cleanly under current `tsconfig.json`.

**test/state.machine.test.ts** — describe name: `"state machine reducer"`. `it.todo` placeholders:
- `"editor-changed from IDLE transitions to CODING and resets startTimestamp (STATE-01)"`
- `"editor-changed from CODING updates filename/language without resetting startTimestamp (STATE-05 invariant)"`
- `"idle-tick from CODING transitions to IDLE and resets startTimestamp (STATE-02)"`
- `"idle-tick from IDLE is a no-op"`
- `"agent-started transitions to AGENT_ACTIVE regardless of prior kind (STATE-03 priority)"`
- `"agent-ended from AGENT_ACTIVE transitions to IDLE (STATE-04)"`
- `"branch-changed preserves kind and startTimestamp (STATE-05 invariant across branch churn)"`
- `"unknown event returns state unchanged (future-proof)"`
- `"buildContext returns immutable snapshot covering all State fields (D-06)"`

**test/rpc.throttle.test.ts** — describe name: `"rpc throttle — leading + trailing + last-wins"`. `it.todo` placeholders:
- `"fires leading edge immediately on first call"`
- `"20 calls across 1s produce exactly 2 underlying calls — leading with first payload, trailing with last (RPC-02 / STATE-06)"`
- `"trailing fire emits the most recent payload, not the one that scheduled the trailer (last-wins, D-10)"`
- `"no trailing fire when only one call lands in the window"`
- `"cancels pending trailer when a new leading edge fires after the window elapses"`

**test/rpc.client.backoff.test.ts** — describe name: `"connection manager backoff"`. `it.todo` placeholders:
- `"login rejection schedules retry at 5s (attempt 0, ladder head)"`
- `"second rejection waits 10s, third 20s, fourth 40s, fifth+ capped at 60s (ladder progression RPC-03)"`
- `"cooldown floor: forced retry at 1s after last attempt waits full 5s, not the ladder value (5s floor)"`
- `"ready event resets attempt counter back to 5s on next disconnect (ladder resets on success)"`
- `"ready event triggers onReady callback for reconnect replay (RPC-04 / D-12)"`
- `"pid is forwarded into setActivity / clearActivity on every call (RPC-01)"`
- `"stop() clears pending retry timeout and prevents late-resolve orphan clients (Phase 1 WR-01 carry-forward)"`
- `"SIGINT handler registered by ConnectionManager unregisters on stop() (Phase 1 WR-04 carry-forward)"`
- `"all connect/setActivity/clearActivity errors swallowed — only console.debug, never console.error (RPC-05)"`

**test/privacy.test.ts** — describe name: `"privacy.redact"`. `it.todo` placeholders:
- `"mode=show returns input unchanged for workspace / filename / branch fields"`
- `"mode=hide returns empty string regardless of field"`
- `"mode=hash throws Error with message \"not implemented until Phase 4\""`
- `"unknown mode treated as show (default-safe)"`

**test/detectors.editor.test.ts** — describe name: `"editor detector"`. Include `vi.mock("vscode", () => ({ window: { activeTextEditor: undefined, onDidChangeActiveTextEditor: vi.fn() }, Disposable: { from: vi.fn((...d) => ({ dispose: () => d.forEach((x: any) => x.dispose?.()) })) } }))` at top level. `it.todo` placeholders:
- `"dispatches editor-changed on construction when activeTextEditor is set"`
- `"dispatches editor-closed on construction when activeTextEditor is undefined"`
- `"dispatches editor-changed with filename + languageId when onDidChangeActiveTextEditor fires"`
- `"dispatches editor-closed when onDidChangeActiveTextEditor fires with undefined"`
- `"returned Disposable removes all subscriptions when dispose() is called"`

**test/detectors.git.test.ts** — describe name: `"git detector"`. Include `vi.mock("vscode", () => ({ extensions: { getExtension: vi.fn() }, Disposable: { from: vi.fn(() => ({ dispose: vi.fn() })) } }))` at top level. `it.todo` placeholders:
- `"dispatches branch-changed with Repository.state.HEAD.name on repository.state.onDidChange"`
- `"silent when getExtension('vscode.git') returns undefined (no crash, no toast, no dispatch)"`
- `"activates vscode.git extension when isActive=false and then reads getAPI(1)"`
- `"multi-repo workspace: picks repository matching activeTextEditor.document.uri.fsPath, falls back to repositories[0]"`
- `"wraps all reads in try/catch; read failure produces no dispatch (silent swallow, D-18)"`

After creating all six files, run `pnpm test` — must exit 0 with all `it.todo` listed as pending.
  </action>
  <verify>
    <automated>test -f test/state.machine.test.ts && test -f test/rpc.throttle.test.ts && test -f test/rpc.client.backoff.test.ts && test -f test/privacy.test.ts && test -f test/detectors.editor.test.ts && test -f test/detectors.git.test.ts && pnpm test 2>&1 | grep -E "(todo|pending|passed)" && pnpm test 2>&1 | tail -1 | grep -Eqv "failed"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/state.machine.test.ts` exits 0
    - `test -f test/rpc.throttle.test.ts` exits 0
    - `test -f test/rpc.client.backoff.test.ts` exits 0
    - `test -f test/privacy.test.ts` exits 0
    - `test -f test/detectors.editor.test.ts` exits 0
    - `test -f test/detectors.git.test.ts` exits 0
    - `grep -c "it.todo" test/state.machine.test.ts` returns >= 9
    - `grep -c "it.todo" test/rpc.throttle.test.ts` returns >= 5
    - `grep -c "it.todo" test/rpc.client.backoff.test.ts` returns >= 9
    - `grep -c "it.todo" test/privacy.test.ts` returns >= 4
    - `grep -c "it.todo" test/detectors.editor.test.ts` returns >= 5
    - `grep -c "it.todo" test/detectors.git.test.ts` returns >= 5
    - `grep -l "vi.mock(\"vscode\"" test/detectors.editor.test.ts` matches
    - `grep -l "vi.mock(\"vscode\"" test/detectors.git.test.ts` matches
    - `pnpm test` exits 0 (all .todo tests reported as pending, none failing)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>All six test stubs created; `pnpm test` green; todo placeholders match the lists above.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend scripts/check-api-surface.mjs with path-scoped vscode-import ban for pure-core</name>
  <files>scripts/check-api-surface.mjs</files>
  <read_first>
    - scripts/check-api-surface.mjs (current implementation)
    - .planning/phases/02-core-pipeline/02-RESEARCH.md §Code Examples §8 (path-scoped regex)
    - .planning/phases/02-core-pipeline/02-CONTEXT.md D-16 (rule text)
    - .planning/phases/01-skeleton-rpc-seam/01-REVIEW.md IN-03 (regex broadening note — fold in if trivial; otherwise explicit defer)
  </read_first>
  <action>
Extend `scripts/check-api-surface.mjs` with a path-scoped `vscode`-import ban. Preserve all existing checks (BAD_CAST, BAD_ANY, PROPOSED_API). Add:

```javascript
// Path-scoped vscode-import ban (D-16). Pure-core modules MUST NOT import vscode at runtime.
// Type-only imports (`import type ...`) are erased by the TS compiler and are allowed, but
// for Phase 2 strictness we ban ALL forms under these paths — pure-core should not even
// need vscode types (State / Event are defined in src/state/types.ts with zero vscode refs).
const VSCODE_RUNTIME_IMPORT = /^\s*import\s+(?!type\b)(?:\*\s+as\s+\w+\s+|\{[^}]*\}\s+|\w+\s+)?from\s+["']vscode["']/m;
const PURE_CORE_PATHS = ["src/state/", "src/rpc/throttle.ts", "src/privacy.ts"];

function isPureCore(file) {
  // Normalize path separators for cross-platform matching.
  const norm = file.split("\\").join("/");
  return PURE_CORE_PATHS.some((p) => norm === p || norm.startsWith(p));
}
```

In the existing `for (const file of files) { ... }` loop, AFTER the BAD_CAST / BAD_ANY checks, add:

```javascript
  if (isPureCore(file) && VSCODE_RUNTIME_IMPORT.test(content)) {
    console.error(`[api-surface] FAIL — ${file} imports vscode at runtime; pure-core boundary violated (D-16)`);
    failed = true;
  }
```

Also: update the final success message to include the pure-core scan count, e.g.:
```javascript
console.log(`[api-surface] PASS — scanned ${files.length} .ts files (${files.filter(isPureCore).length} pure-core), no proposed-API / (vscode as any) / pure-core vscode-import violations`);
```

Also broaden BAD_CAST per 01-REVIEW IN-03 (low-cost fold-in):
```javascript
const BAD_CAST = /\bvscode\s+as\s+(?:unknown\s+as\s+)?any\b/;
```
(Remove the old `BAD_CAST` constant; replace with the broader form above.)

Add a defensive `node_modules`/`dist` guard to `walk()` per 01-REVIEW IN-04:
```javascript
function walk(dir) {
  const out = [];
  if (!existsSync(dir)) return out;
  for (const entry of readdirSync(dir)) {
    if (entry === "node_modules" || entry === "dist" || entry === "build-shims" || entry.startsWith(".")) continue;
    const full = join(dir, entry);
    const s = statSync(full);
    if (s.isDirectory()) out.push(...walk(full));
    else if (full.endsWith(".ts")) out.push(full);
  }
  return out;
}
```

Before finishing: run `pnpm check:api-surface` — it must still PASS on current Phase 1 `src/` (no pure-core files exist yet, so PURE_CORE count is 0; BAD_CAST broadening does not add new matches because `src/rpc/client.ts` + `src/extension.ts` have zero `vscode as any` usage per 01-02 SUMMARY verification evidence).

Then add a temporary poison file `src/state/_poison.ts` with `import * as vscode from "vscode";` and re-run `pnpm check:api-surface` — it MUST exit 1 with the pure-core violation message. Delete the poison file after proving the check works. Commit the poison-proof test result as a verification artifact (cite in your commit message), then ensure `pnpm check:api-surface` is green on the final committed state.
  </action>
  <verify>
    <automated>pnpm check:api-surface && grep -q "PURE_CORE" scripts/check-api-surface.mjs && grep -q "VSCODE_RUNTIME_IMPORT" scripts/check-api-surface.mjs && grep -q "pure-core boundary violated" scripts/check-api-surface.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check:api-surface` exits 0 on the final committed state
    - `grep -c "PURE_CORE_PATHS" scripts/check-api-surface.mjs` returns >= 1
    - `grep -c "src/state/" scripts/check-api-surface.mjs` returns >= 1
    - `grep -c "src/rpc/throttle.ts" scripts/check-api-surface.mjs` returns >= 1
    - `grep -c "src/privacy.ts" scripts/check-api-surface.mjs` returns >= 1
    - `grep -E "pure-core boundary violated|D-16" scripts/check-api-surface.mjs` matches at least once
    - `grep -q "node_modules" scripts/check-api-surface.mjs` — walk() guards against node_modules descent
    - Poison-test (temporary `src/state/_poison.ts` with `import * as vscode from "vscode"`) causes `pnpm check:api-surface` to exit 1 with "pure-core boundary violated" in stderr — verified manually during task, deleted before commit
    - Final `ls src/state/ 2>/dev/null` returns empty or nonexistent (no state dir yet)
  </acceptance_criteria>
  <done>check-api-surface.mjs extended with path-scoped ban; poison-test proves the guard works; final state green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author 02-HUMAN-UAT.md with three manual dev-host verification checklists</name>
  <files>.planning/phases/02-core-pipeline/02-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/01-skeleton-rpc-seam/docs/HUMAN-HANDOFF.md (Phase 1 UAT template — if present; match the style)
    - .planning/phases/02-core-pipeline/02-VALIDATION.md §Manual-Only Verifications
    - .planning/phases/02-core-pipeline/02-RESEARCH.md §Pitfall 7 (two-window pid isolation on older Discord)
    - .planning/ROADMAP.md Phase 2 Success Criteria 1, 3, 4
  </read_first>
  <action>
Create `.planning/phases/02-core-pipeline/02-HUMAN-UAT.md` covering the three manual-only behaviors from 02-VALIDATION.md § Manual-Only Verifications. Use this exact structure:

```markdown
---
phase: 02
doc: human-uat
status: pending
created: 2026-04-13
---

# Phase 2 — Dev Host Manual Verification

Three behaviors that cannot be automated in vitest. Run ALL three after Phase 2 code is green in CI. Check each box and sign off at the end.

## Prerequisites

- [ ] Discord desktop installed and logged in (any Discord >= 2023; pid-scoping was introduced in ~2021 but the incumbent bug pattern was fixed mid-2022).
- [ ] `pnpm build` is green on the branch under test.
- [ ] `pnpm test`, `pnpm typecheck`, `pnpm check:bundle-size`, `pnpm check:api-surface` all green.
- [ ] One workspace folder with at least one `.ts` or `.js` file ready to open.

## Checklist 1 — IDLE transition on timer (SC-1)

**Requirement:** STATE-02 — No focused editor + `idleTimeoutSeconds` elapsed → IDLE.

**Why manual:** `idleTimeoutSeconds` is hardcoded to 300_000 ms in Phase 2; vitest uses fake timers but wall-clock observation confirms the driver uses real `setTimeout`, not a test-only shortcut.

1. Launch Dev Host (F5 from repo root).
2. Open any `.ts` file in the Dev Host workspace.
3. Verify Discord friends sidebar shows activity with the filename in the state string within 2 s (CODING state).
4. Close the file tab. Focus the terminal. Leave all editors closed.
5. Start a timer on your phone for **5 minutes 10 seconds**.
6. Wait. Do not re-focus an editor.
7. At timer completion: verify Discord friends sidebar shows IDLE copy (Phase 2 renders plain "idle" — no goblin messages until Phase 4).
8. Re-focus an editor — verify presence flips back to CODING immediately.

- [ ] Step 3 — CODING shown with filename ___________________________
- [ ] Step 7 — IDLE shown after 5 min elapsed _______________________
- [ ] Step 8 — CODING restored on editor re-focus ___________________

## Checklist 2 — Discord kill/restart replay (SC-3)

**Requirement:** RPC-03 + RPC-04 — 5→10→20→40→60 s backoff with 5 s cooldown floor; activity replays within one backoff tick on Discord restart.

**Why manual:** Requires actual Discord desktop process lifecycle; vitest mocks the socket.

1. Launch Dev Host, open a file, verify CODING appears in Discord sidebar.
2. Open Dev Host Output panel; select channel `Log (Extension Host)` (Phase 2 logs via `console.debug` — Phase 4 migrates to a dedicated channel).
3. Kill Discord desktop entirely:
   - macOS: `killall Discord`
   - Linux: `pkill Discord`
   - Windows: Task Manager → end `Discord.exe`
4. Observe Output: within ~5 s you should see a `[agent-mode-discord] RPC connect failed` or disconnected debug line, then repeated connect attempts at 5 s, 10 s, 20 s spacing.
5. After ~30 s of failed retries, relaunch Discord desktop.
6. Watch the Output log — within the current ladder tick (≤ 60 s from Discord relaunch) you should see a success line OR the activity re-appears in the Discord friends sidebar without clicking anything in the editor.

- [ ] Step 3 — Discord killed __________________________
- [ ] Step 4 — Retry cadence matches ladder (note timestamps): ___, ___, ___
- [ ] Step 6 — Activity replays within one backoff tick after Discord relaunch _____
- [ ] No two connect attempts within 5 s of each other (verify from timestamps above)

## Checklist 3 — Two-window pid isolation (SC-4)

**Requirement:** RPC-01 — Two VS Code windows open simultaneously produce two independent Discord activities; closing one does not clear the other.

**Why manual:** Requires two real VS Code Dev Host windows + real Discord with pid-scoping honored (see 02-RESEARCH Pitfall 7 — older Discord clients don't honor pid; test on current Discord).

1. Close all Dev Host windows. Launch Dev Host (F5).
2. In the first window, open a file (`a.ts`). Verify Discord shows one activity with `a.ts` in the state string.
3. Launch a second Dev Host (Cmd/Ctrl+Shift+P → "Developer: New Window"; then F5 from a second clone of the repo, or open a second folder in the existing Dev Host).
4. In the second window, open a different file (`b.ts`).
5. **Observe Discord friends sidebar on your Discord mobile or a second Discord desktop account** (Discord shows YOUR own activity as one entry — the pid-scoped second activity is visible to friends, not to you. Ask a friend or use a test account).
6. Confirm two distinct activities, one with `a.ts`, one with `b.ts`, both under "Visual Studio Code" or the branded app name.
7. Close the first window. Wait 5 s.
8. Verify the second window's activity (`b.ts`) remains intact; only the `a.ts` activity disappears.

- [ ] Step 6 — Two independent activities visible (screenshot encouraged) ____
- [ ] Step 8 — Closing window 1 does not clear window 2's activity ____

**Note on Discord version:** If step 6 shows only one activity (the most recent window wins), you may be on an older Discord client that does not honor pid-scoping. This is NOT an extension bug — document the Discord version in the sign-off.

## Sign-off

| Check | Status | Observer | Date |
|-------|--------|----------|------|
| Checklist 1 (SC-1 / STATE-02) | | | |
| Checklist 2 (SC-3 / RPC-03, RPC-04) | | | |
| Checklist 3 (SC-4 / RPC-01) | | | |

Discord client version tested: __________________
OS / VS Code version tested: __________________

Signed off by: __________________ on ______________
```

After writing the file, verify it renders readably (markdown lint optional — no linter configured in Phase 1-2).
  </action>
  <verify>
    <automated>test -f .planning/phases/02-core-pipeline/02-HUMAN-UAT.md && grep -q "Two-window pid isolation" .planning/phases/02-core-pipeline/02-HUMAN-UAT.md && grep -q "STATE-02" .planning/phases/02-core-pipeline/02-HUMAN-UAT.md && grep -q "RPC-03" .planning/phases/02-core-pipeline/02-HUMAN-UAT.md && grep -q "RPC-04" .planning/phases/02-core-pipeline/02-HUMAN-UAT.md && grep -q "RPC-01" .planning/phases/02-core-pipeline/02-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/02-core-pipeline/02-HUMAN-UAT.md` exits 0
    - `grep -E "Checklist 1|Checklist 2|Checklist 3" .planning/phases/02-core-pipeline/02-HUMAN-UAT.md` returns 3 matches
    - `grep -q "killall Discord\|pkill Discord\|Task Manager" .planning/phases/02-core-pipeline/02-HUMAN-UAT.md` — kill commands documented
    - `grep -q "5 minutes" .planning/phases/02-core-pipeline/02-HUMAN-UAT.md` — idle wait duration
    - `grep -q "Sign-off" .planning/phases/02-core-pipeline/02-HUMAN-UAT.md`
    - Frontmatter present (`grep -q "^phase: 02" .planning/phases/02-core-pipeline/02-HUMAN-UAT.md`)
  </acceptance_criteria>
  <done>02-HUMAN-UAT.md exists with three verifiable checklists mapped to SC-1, SC-3, SC-4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI ↔ repo files | `scripts/check-api-surface.mjs` is the enforcement point; a gap here means pure-core can silently take a runtime dep on vscode |
| Wave-0 stubs ↔ downstream task `<automated>` refs | Missing test file means downstream tasks auto-fail the Nyquist gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Tampering | `scripts/check-api-surface.mjs` regex | mitigate | Broaden `VSCODE_RUNTIME_IMPORT` to match `import`, `import *`, `import { }`, `import name` forms (D-16 is strict at runtime; type-only imports erased at build so not a violation in practice); poison-test proves the guard fires |
| T-02-00-02 | Repudiation | Path-scoped ban silently misses a file due to path separators | mitigate | `isPureCore` normalizes `\` → `/` before match; test on macOS + lintable by CI matrix when Phase 5 lands ubuntu/macos/windows |
| T-02-00-03 | Denial of Service | `walk()` recurses into `node_modules` if someone redirects SRC_DIR | mitigate | Explicit deny-list in `walk()` skips `node_modules`, `dist`, `build-shims`, dotfiles (per 01-REVIEW IN-04) |
| T-02-00-04 | Information Disclosure | 02-HUMAN-UAT.md leaks personal Discord account name | accept | Checklist 3 suggests using a test account / friend view; no secrets in repo |
| T-02-00-05 | Elevation of Privilege | Test file executes vscode types that leak to prod | accept | vi.mock("vscode", …) intercepts at module-graph level; no vscode runtime reaches tests |
</threat_model>

<nyquist>
This is Wave 0. No `<automated>` dependencies on prior waves.
Downstream waves reference these test files:
- 02-01 → `test/state.machine.test.ts` (created here)
- 02-02 → `test/state.machine.test.ts -t "context"` (extends this file; created here)
- 02-03 → `test/rpc.throttle.test.ts` (created here)
- 02-04 → `test/rpc.client.backoff.test.ts` (created here)
- 02-05 → `test/privacy.test.ts` (created here)
- 02-06 → `test/detectors.editor.test.ts` (created here)
- 02-07 → `test/detectors.git.test.ts` (created here)
All six files present in repo after this plan + `scripts/check-api-surface.mjs` extended for D-16 + `02-HUMAN-UAT.md` in place for SC-1/3/4 signoff at phase gate.
</nyquist>

<verification>
Run the full guardrail suite after all three tasks commit:

```bash
pnpm test && \
pnpm typecheck && \
pnpm build && \
pnpm check:bundle-size && \
pnpm check:api-surface
```

All five commands must exit 0. `pnpm test` will report 37+ tests as "todo" / "pending" — this is expected and correct (they land in 02-01..02-07).
</verification>

<success_criteria>
- 6 test stub files present, each with the `describe` name and `it.todo` placeholders enumerated above
- `scripts/check-api-surface.mjs` contains `PURE_CORE_PATHS`, `VSCODE_RUNTIME_IMPORT`, `isPureCore`, and the new FAIL message
- Poison-test verified (manually during task 2) — writeup in commit message
- `.planning/phases/02-core-pipeline/02-HUMAN-UAT.md` covers SC-1, SC-3, SC-4 with sign-off matrix
- `pnpm test && pnpm typecheck && pnpm build && pnpm check:bundle-size && pnpm check:api-surface` all green
- Zero new runtime deps, zero new devDeps
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pipeline/02-00-SUMMARY.md` covering:
- What test stubs landed (file list with `it.todo` counts)
- Final `scripts/check-api-surface.mjs` shape (new constants, new FAIL path)
- Poison-test result (exit code + stderr excerpt proving the guard works)
- 02-HUMAN-UAT.md checklist count + sign-off matrix present
- Provides: "All Wave 0 test files present; pure-core import ban enforced in CI; SC-1/3/4 dev-host checklist ready for phase gate"
- Next plan readiness: 02-01 / 02-03 / 02-05 can now run in parallel (Wave 1); each references its test file by the `it.todo` name for the first test-to-green cycle
</output>
